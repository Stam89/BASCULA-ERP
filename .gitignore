node_modules/
dist/
build/
.gradle/
local.properties
.env
.env.*
!.env.example
.idea/
.vscode/
*.apk
*.aab
*.keystore
*.jks
*.log
logs/
coverage/
backend/uploads/
.claude/

# Credencial de Firebase (no subir)
backend/scripts/firebase-key.json
firebase-key.json

# Logs del servidor (se generan solos, no son código)
backend-log.txt
*.log
logs/

# Secreto de firma de sesiones (se genera solo, nunca se versiona)
.jwt-secret

# API key del endpoint externo de báscula (se genera sola, nunca se versiona)
.external-api-key

# Carpeta de respaldo previa a sync (no versionar)
.respaldo-antes-sync-20260805/
