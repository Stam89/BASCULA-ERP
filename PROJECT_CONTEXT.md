# PROJECT_CONTEXT — BASCULA-ERP

> Memoria compacta para continuar sin releer todo. Última actualización: 2026-09-04 (**Gana: Costo Prod usa tarifa PILADO del SOCIO (tarifario_servicio, $3.75), no labor_rates**). Todo en main `de36377`. (Antes 2026-09-04 `a73fd05` = Gana tarifa pilada + Total cáscara=(Precio+CostoProd)×QQ; `df00a43` = Gana % Blanco excedente; `55c05c3` = Fomentos folio/editar/eliminar.)
> Al empezar una sesión, **lee solo este archivo** primero.
> Nota: el checkout de trabajo/despliegue es el **MAIN** (`C:\Users\Usuario\OneDrive\Documentos\GitHub\BASCULA-ERP`). Ignorar cualquier worktree en `.claude/worktrees/*` (están sobre ramas viejas).

## 0. Stack / cómo corre
- Backend: Node/Express + TS (ESM/NodeNext strict), PostgreSQL 18 (`pg`), zod. Puerto **:4000**, sirve `web-admin/dist`.
- Frontend: React 18 + Vite 6. **Un solo archivo gigante**: `web-admin/src/App.tsx` (~12.7k líneas). Helpers en `web-admin/src/format.ts` (`money`, etc.).
- Migraciones propias: `cd backend && npm run db:migrate` (archivos en `database/migrations/`, registradas en `schema_migrations`). Aditivas siempre (`IF NOT EXISTS` / `ADD COLUMN IF NOT EXISTS`).
- BD local: `postgres://postgres:1989@localhost:5432/bascula_erp`. psql en `C:/Program Files/PostgreSQL/18/bin/psql.exe`.
- Verificación: FE `node ./node_modules/typescript/bin/tsc --noEmit` + `vite build`; BE `npx tsc --noEmit` / `npm run build`.
- **Se trabaja y despliega desde el checkout MAIN** (`C:\Users\...\BASCULA-ERP`), rama `main`, commit+push cada fase. El worktree `determined-proskuriakova-259d21` está sobre una rama vieja/divergente: **ignorarlo**.
- **DESPLIEGUE DEL BACKEND (importante):** el proceso en :4000 corría como **`node dist/server.js`** (build COMPILADO, via `npm start`), **NO** `tsx watch`. ⇒ Los cambios de backend **no se reflejan solos**: hay que `cd backend && npm run build` (tsc→dist) y **reiniciar** el proceso (`Stop-Process -Id <pid> -Force` del PID de :4000; luego `cd backend && npm start` en background). Confirma el modo con `Get-CimInstance Win32_Process -Filter "ProcessId=<pid>"` → `CommandLine`. El frontend igual: `cd web-admin && npm run build` + hard refresh. (Esto es para DEV local en Windows.)
- **DESPLIEGUE A PRODUCCIÓN AUTOMATIZADO (server, `14c8fae`):** hay un script único **`./deploy.sh`** (raíz) que corre, en orden: (1) `cd backend && npm run db:migrate` (migraciones aditivas pendientes), (2) `cd backend && npm run build`, (3) `cd web-admin && npm run build`, (4) `pm2 reload ecosystem.config.js --env production || pm2 start ...`. Config PM2 en **`ecosystem.config.js`** (raíz): app `bascula-erp`, `cwd:./backend`, `script:dist/server.js`, **modo `fork`** (1 instancia; NO cluster — hay pool PG + estado en memoria), `max_memory_restart:500M`, `env_production:{NODE_ENV:production, PORT:4000}`. **Prerrequisitos del server (una vez):** `npm i -g pm2`; **`backend/.env`** con `DATABASE_URL` (obligatoria en prod; opcional `JWT_SECRET`/`CORS_ORIGINS`/`PORT`) — los secretos se cargan de ahí vía dotenv, NO van en git ni en el ecosystem; tras el 1er `pm2 start`, correr **`pm2 save`** (y opcional `pm2 startup` para sobrevivir reinicios del server). **Flujo de deploy:** en el server `git pull` → `./deploy.sh`. `.gitattributes` fuerza LF en `*.sh` (con CRLF el shebang falla en Linux). Sigue SIN auto-deploy: `deploy.sh` se corre a mano.

## 1. ESTADO ACTUAL
ERP para piladora de arroz con arquitectura **multi-accionista**: MATRIZ **CEYRO** (dueña, id `00000000-0000-0000-0000-000000000001`) y SOCIOS **ROVINSON** y **STALYN**. Selector de accionista activo manda header `X-Accionista-Id`; permisos por accionista.
- Funciona: Dashboard, Báscula, Secadoras, Producción, **Gana** (nuevo, liquidación de rendimiento), Inventario, Selección/envejecido, Ventas (pedido→despacho), Compras, Caja, Por Cobrar, Por Pagar, Liquidaciones, Fomentos, Agricultores, Nómina, Cuadrilla, Servicio Pilado, Estados Financieros, Costos Operativos, Reportes, Configuración.
- **main** commiteado+pusheado (último `f79221e`: **módulo Gana** — ver §2r; antes `4264463` cuadrilla por túnel §2q). `App.tsx` ya va por ~14k líneas.
- **NUEVO 2026-09-03 (todo en main, ver §2r):** empaque **Tulas/Sacos por túnel** (Secadoras) y **MIX Tula/Saco por línea** en Pilado (inventario + nómina del estibador combinan tula+saco); **sacos = inventario EXCLUSIVO de la Matriz** (descuento centralizado + trazabilidad "Lote/Socio", oculto a socios, API con guard `assertMatriz`); **Caja↔Inventario de Sacos** (egreso "Compra de sacos" genera ENTRADA automática + reverso al anular; enlace `sack_movements.ref_cash_movement`); **precio_compra_default** por tipo de saco (autocompleta en Caja, editable en Inventario); **cuadro de rendimiento por lote** (snapshot `production_yields.rendimiento_snapshot`) y **módulo «Gana»** (liquidación por accionista, fórmulas de división directa). Migraciones `20260914`…`20260917` aplicadas en local.
- **NUEVO 2026-09-02 (todo en main):** (a) **Homologación de agricultores** — tabla `agricultor_alias`, el import de báscula resuelve nombre exact→alias→null (NO auto-crea), UI para vincular tickets desconocidos, y **API externa** `GET /api/v1/external/agricultores` protegida por API Key (`backend/.external-api-key`, autogenerada, fuera de git). (b) **Nómina→Cuadrilla: pago automático por Recepción/Botada de túnel** — botón **🔍 Detectar labores de túnel** que auto-asigna la cuadrilla activa y **registra directo** en "Registros del período" (QQ del túnel × tarifa), marcados 🔒🤖. Sin cola de pendientes. (c) **Sincronización de corrida por motor** y **detección del secador por corrida** (no por día calendario). Detalle en §2q. Menú Campo: Caja·Servicios·**Clientes**·Vales·Partes Diarios·Reportes·Configuración. La sección Caja tiene barra de sesión (apertura/arqueo/cierre) + pill "Cierres de Caja". La sección Clientes tiene **conciliación de fletes** (créditos a favor + convertir parte). Typecheck/build FE+BE en verde. Backend corre compilado desde `dist/`. **No hay auto-deploy** (el `ci.yml` solo hace typecheck/tests/build/lint en runners de GitHub, sin BD; producción se despliega a mano). **La máquina local ya corre `e1b7396` con sus migraciones aplicadas** (se desplegó al verificar).
- **En main ya está el módulo Caja de Campo completo hasta V3 + Flota/Maquinaria + Egreso/Ingreso estandarizados + Vales + Partes Diarios de Cosecha (con cobro)**: V1 captura, V2 reportes (vista `campo_servicios_saldo` + 3 reportes), Campo como **operación de CEYRO** en el selector de arriba (no pastilla de sidebar), **contexto AISLADO** (layout propio `CampoWorkspace`, menú `CAMPO_SECCIONES`: Caja·Servicios·Vales·**Partes Diarios**·Reportes·Configuración) y **V3** (nombre de operación editable en `campo_config`, sección **Caja** con ingreso/egreso/transferencia/libro con saldo corrido). Migraciones aplicadas en local hasta `20260829_campo_partes_cobro.sql`. BD local **limpia** (0 movimientos/partes; semillas: 3 cuentas, 8 categorías; flota intacta).
- **Flota y Maquinaria** (`1b1e394`): `campo_activos` extendida con `placa_codigo`+tipos de flota; CRUD "🚜 Flota y Maquinaria" en Configuración; selector de máquina en Egreso; badge en el Libro. Migración `20260828_campo_maquinaria.sql`.
- **Egreso reordenado + Vales/Anticipos** (`b904b4c`): egreso con orden estricto (Fecha·Máquina*·Categoría*·Concepto·Monto·Cuenta), máquina OBLIGATORIA con default "🏢 Gastos Generales / Administración"; **Vales por rendir** (sección 📋, estado + ajuste `naturaleza='ajuste_vale'`). Migración `20260828_campo_vales_anticipos.sql`. Ver §2k.
- **Conceptos estandarizados** (`e5ea62b`/`ae08f9d`/`6a1696c`): en el EGRESO REPARACION_MANT el Concepto se arma con 2 selects **Pieza (10 optgroups/41 piezas) + Acción** → "Pieza - Acción" (editable); en el INGRESO el Concepto es un select `CONCEPTOS_INGRESO` (3 grupos/7 opciones) + detalle extra que concatena. Ver §2l.
- **Partes Diarios de Cosecha** (`2a81f75` persistencia + `91bcc21` cobro): sección/módulo `PartesModule.tsx` para el reporte de operadores. Tabla `campo_partes` (fecha, activo_id, operador, cliente, qq, observaciones, estado). Botón **"💵 Generar cobro"** → crea un `campo_servicio` (cosecha, valor=qq×precio) y marca el parte `cobrado`; el cobro real sigue con la maquinaria de Servicios/Por Cobrar. Migraciones `20260829_campo_partes.sql` y `20260829_campo_partes_cobro.sql`. Ver §2l.

## 2. CAMBIOS DE ESTA SESIÓN (2026-08-20)
1. **Ventas** — Toma de pedido en split-view 2 columnas + "Cola de carga" para bodega (badge 🟡, texto grande de sacos). Solo UI; estado sigue `PENDING`/`DELIVERED`. `4a2d90a`.
2. **Costos Operativos** — movido de Producción a nueva pestaña **Contabilidad → Costos Operativos** (mismo form/tabla/endpoints `/costos`). `c838994`.
3. **Cargo automático por empaque** — al DESPACHAR un pedido de un SOCIO, CEYRO le cobra por sacos 10/25/50 lb. Migración `20260822`, `backend/src/services/cargo-empaque.ts`, hook en `orders.ts /:id/deliver`, config en Configuración→Tarifas. `c9bfcc0`. **Tarifas ya definidas (2026-08-21): 10lb=$0.20, 25lb=$0.22, 50lb=$0.27** (fila de CEYRO en `matriz_packaging_rates`). Editables en Configuración→💲Tarifas→"📦 Tarifas de empaque" (admin, `PUT /settings/packaging-rates`).
4. **Mantenimiento dinámico** — Áreas/Secciones/Tipos ahora en tabla `maintenance_categories` (migración `20260823`), con botón **[+ Nueva]** + modal en Caja→Mantenimiento. Reemplaza la constante estática `SECCIONES_POR_AREA`. `d3607b8`.
5. **Mantenedor de categorías** — Configuración → "🔧 Categorías mant.": renombrar (propaga a histórico) y desactivar (soft-delete). `b6ed200`. Filtros del historial muestran inactivas con sufijo "(Inactiva)"; los forms de creación solo activas. `76fd0e5`.
6. **Por Cobrar** — rediseño: tarjetas **consolidadas por deudor** + modal "estado de cuenta" (tabla, Pagar total / Abono parcial, Imprimir). Backend resuelve nombre del deudor socio. `95cd1fc`.
7. **Por Pagar** — mismo patrón, simétrico. Modal e impresión unificados en `CuentaDetalleModal` / `printCuentaStatement`. Backend resuelve nombre del acreedor. Se borró código muerto (`CuentaCardNueva`, `groupPayables`). `50b60d0`.

Archivos tocados: `web-admin/src/App.tsx` (todo); `backend/src/routes/modules/{orders,settings,equipment,receivable,cash}.ts`; `backend/src/services/{cargo-empaque,cuentas-vinculadas}.ts`; `database/migrations/2026082{2,3}_*.sql`.

## 2b. CAMBIOS 2026-08-21 (continuación)
8. **Ventas — fixes:** (a) encabezado de "Ventas realizadas" con `color:#fff`+`bold` inline en cada `<th>` (una regla global de `th` pisaba la herencia); (b) recibo mostraba la cantidad mal (5 en vez de 50): `sale_items.quantity` guarda QQ convertidos pero precio/total son por saco → el recibo ahora muestra `total/precio` y la columna se llama "Cantidad". `fd79df9`.
9. **Guía de Remisión** — migración `20260824` (campos transportista_nombre/cedula, vehiculo_placa, guia_number, guia_emitida_at en `sales_orders` + secuencia `guia_remision_seq`, formato `001-001-000000001`). `PUT /orders/:id/guia` guarda transporte y asigna nº la 1ª vez (solo pedidos DELIVERED; no toca inventario/contab.). Modal captura Chofer/Cédula/Placa + impresión **A4** (`@page A4` + `@media print`, ventana limpia): header matriz + recuadro guía, grid Remitente/Destinatario/Transportista, tabla Cantidad/Presentación/Producto (sin precios), 3 firmas. `7370651`.
10. **Guía — reubicación UX:** el botón vive ahora en la tabla "Ventas realizadas" (columna "Recibo"→"Documentos", junto al 🖨), con columna "Guía N°". Se eliminó la tabla "Pedidos despachados". La venta se enlaza a su pedido por `sale_id`. `3acc709`.
11. **Inventario → dashboard:** cabecera con **[⚖️ Ajuste/Cuadre Manual]** (modal) y **[📄 Ver Kardex/Movimientos]** (drawer lateral); el form de cuadre y el reporte de movimientos salen de la vista principal. Fila de **KPIs en QQ** (Total Cáscara/Producto Terminado/Subproductos) + tablas como tarjetas. Sin tocar queries/lógica: el modal reusa `submitStockAdjustment`. `89f8b97`.
12. **Dashboard — 7 KPIs por accionista:** el Panel Integral (`PanelIntegral`) filtra sus 7 tarjetas por el accionista activo (deriva de `per_accionista` + `*_por_acc` que ya vienen en `/dashboard/panel`; sin cambiar fórmulas base). Subtítulo dinámico "Mostrando datos de: NOMBRE". Servicio de pilado y Costo Operativo son propios de la MATRIZ → un socio los ve en $0. Verificado en vivo. `2b96e13`.
13. **Compra múltiple de sacos (carrito):** Caja→Sacos ahora es un carrito — [➕ Agregar a la lista] acumula ítems, tabla Tipo/Cantidad/Precio/Subtotal/🗑️ + TOTAL GENERAL, y [💾 Confirmar y Registrar Compra] envía todo. Backend `POST /sacks/purchases` recibe `items[]` (acepta el formato de 1 saco por compat.), en UNA transacción itera stock+kardex por ítem y hace UN solo egreso consolidado ("Compra de múltiples sacos", reference_id NULL). Archivos: `backend/src/routes/modules/sacks.ts` + App.tsx. `1756de7`. Probado en vivo (compra de 2 tipos → 1 egreso $11.60). **Fix:** el `<select>` de tipo de saco salía vacío en Caja→Sacos; ahora `refreshSacks()` corre al abrir esa subpestaña. `a9285b8`.
14. **Sacos SIEMPRE de la matriz al despachar:** solo CEYRO posee inventario de sacos (`sack_inventory` es tabla ÚNICA, sin accionista_id). Al despachar, el arroz/subproductos salen del inventario del socio (crearVenta, sin cambios) pero los SACOS se descuentan del inventario de la matriz. Nueva `descontarSacosDelDespacho()` en `cargo-empaque.ts` (por presentación con peso → tipo "Saco N LB", movimiento SALIDA; no bloquea si falta stock o no existe el tipo), llamada en `orders.ts /:id/deliver` junto al cargo por empaque, misma transacción. Verificado en vivo: pedido ROVINSON 5×10lb → Saco 10 LB 112→107, arroz −0.5 QQ de ROVINSON, CxC/CxP $1.00. `6d12837`.
15. **Báscula = Bandeja de entrada (Inbox):** se quitó de la vista el form "Registrar ingreso" (ahora modal via [+ Ingreso Manual (Emergencia)] en la cabecera de la tabla de tickets) y las secciones "Últimos lotes" y "Pasar un lote". La tabla de Tickets es full-width y el centro. Filtro por defecto ya era "Pendientes" (se conserva; "Todos" muestra historial). Se mantiene en Báscula la "Corrección puntual de accionista de materia prima". Reubicados: "Últimos lotes" → **Producción** (DataList); "Pasar un lote" → **Inventario**, botón [🔄 Transferir Lote] en la cabecera → modal (solo si accionistas>1). `fb51167`.
16. **Selección más limpia:** "Personas externas" (form+tabla) → modal via botón [👤 Gestionar Personas Externas] junto al selector. "💲 Tarifas por defecto" → **Configuración → Tarifas → tarjeta "Tarifas de Procesos (Selección/Envejecido)"** (mismo `saveSelectionRates`; `refreshConfig` ahora carga `/selection/rates`). El form "Mandar a selectar" sigue leyendo `selectionRates` de la BD para autocompletar "Tarifa por QQ". "Completados" pasó a panel full-width. `ffe0665`.
17. **Sacos: consumo físico al EMPACAR, no en la venta (corrección de flujo).** El saco se gasta al cerrar Producción/Selección, NO al despachar (antes se descontaba en la venta → desfase/duplicidad). `descontarSacosDelDespacho` → helper genérico `descontarSacosPorPeso(client, Map<peso,sacos>, concepto)` en `cargo-empaque.ts` (descuenta de `sack_inventory` = matriz, movimiento SALIDA; permite negativo). Producción (`processing.ts` cerrarProcesoProduccion): sacos según `white_rice_presentations` (sacos = QQ*100/sack_weight_lb). Selección (`selection.ts` finish): nueva presentación POR LÍNEA — migración `20260825` (presentation + sack_weight_lb en `selection_batch_outputs`), UI con selector 100/50/25/10 LB por salida (deshabilitado en rechazo); descuenta exacto por tipo. `orders.ts /:id/deliver` YA NO descuenta sacos (mantiene el CARGO financiero por empaque + baja de producto terminado del socio). Verificado en vivo: cierre selección 2 QQ en Saco 25 LB → 8 sacos (8→0). Commits `3d8a241`, `55dbe8f`.
18. **Secadoras (UI):** selector de Motor y "➕ Agregar al lote" como botones secundarios reales; tarjetas de Secadora con sombra/borde/padding. El bloque de combustible (Bombona/Cilindro/Diésel + totales) sale de la vista principal (modo crear) y vive en un MODAL que abre "✅ Finalizar secado del Motor X" → [Confirmar y Finalizar Secado] reusa los handlers (cerrarCombustibleMotor si hay medidores, si no finalizarSecadoMotor). Prorrateo intacto. `2f8a451`. Botones finales en fila/centrados con jerarquía: "Guardar informe" outline azul (secundario), "Finalizar secado" verde (principal). `ec1b8cc`, `50a2511`.
19. **Producción (UI):** layout de 2 columnas (grid existente `productionModuleGrid` + `align-items:start`): IZQUIERDA (materia prima) = "Secadora en producción" arriba + "Últimos lotes" debajo; DERECHA (procesamiento) = "Reporte de pilado". Dentro del reporte se separó **👥 Personal de Turno** (Pilador/Estibador/Polvillo, fondo tenue) de **⚖️ Rendimiento de Pilado** (N.º tulas, Total QQ, presentaciones, subproductos). Botones: "Guardar Proceso" outline azul (secundario), "Finalizar Lote" verde (principal). Solo layout/estilos; sin tocar cálculos de tulas/quintales. Verificado en vivo. `169e8dd`.

## 2c. CAMBIOS 2026-08-22 (esta sesión)
20. **Inventario = Dashboard de Bodega (limpieza):** la tabla estática "Productos" salió del grid → botón **[⚙️ Catálogo de Productos]** (junto a Ajuste/Cuadre) que abre modal de solo-consulta. La sección **👥 Clientes** se movió de Inventario a **Ventas** (no pertenecía a bodega). `e5fdbaf`.
21. **Inventario — pulido 2×2:** las 3 tablas de stock (cáscara/terminado/subproductos) perdieron la columna "Bodega" (redundante, ya categoriza el título → headers `["Producto","Cantidad"]`). Grid explícito de 2 columnas y la card **📦 Inventario de Sacos** ocupa el 4º cuadrante (balancea el hueco). "Otros stocks" (condicional) queda full-width debajo. `e80bba9`.
22. **Clientes — datos fiscales para la Guía de Remisión:** `customers` YA tenía `identification`/`address` pero no se capturaban ni imprimían. Ahora: **Backend** — `GET /customers` devuelve `address`; nuevo **`PUT /customers/:id`** (edita nombre/tel/RUC/dirección/tipo; RUC duplicado→409); `GET /orders` (lista+detalle) exponen `customer_identification`/`customer_address` vía JOIN. **Frontend** — form de alta con campos RUC/Cédula + Dirección; lista pasó a tabla con RUC/CI, Dirección y botón **✏️ Editar**→modal (completa datos de clientes existentes); la Guía imprime RUC/CI y Dirección reales (antes líneas en blanco), conservando la nota del pedido como "Dirección de entrega". Verificado en vivo por API (GET trae address, PUT guarda y persiste). `5fa170e`.

Archivos tocados (2c): `web-admin/src/App.tsx`; `backend/src/routes/modules/{customers,orders}.ts`.

## 2d. CAMBIOS 2026-08-22 (sesión UI/POS — solo frontend)
23. **Ventas (POS):** Paso 3 "Detalle del pedido" (carrito) **siempre visible** — antes se ocultaba vacío y el layout saltaba del Paso 2 al 4; ahora muestra empty state ("🛒 No hay productos en el pedido aún"). El bloque **👥 Clientes** (alta + tabla maestra) salió de la vista y pasó a un **modal**, abierto con nuevo botón **[👥 Gestionar]** en el Paso 1 (junto a **[+ Nuevo]**, que sigue siendo el alta rápida). El layout 2 columnas (izq pasos 1-2 / der pasos 3-4) ya existía. Sin tocar lógica del carrito/total/API. `ecb820b`.
24. **Caja — menú interno a pills horizontales:** `.cajaSubNav` (styles.css) pasó de `flex-wrap:wrap` (se apilaba en varias filas → alto) a **una sola fila con `overflow-x:auto`**, botones tipo **pills** (`border-radius:999px`): activa **verde esmeralda `#059669`** + texto blanco; inactivas fondo tenue + texto gris (`--c-text-muted`). Tabla queda pegada debajo (compacto). Solo estilos; sin tocar estados/cálculos/guardado ni las tarjetas de saldos. `ecd22f2`, `718c85e`.
25. **Por Cobrar — copy:** botón principal de tarjetas de deudores dice **"📄 Ver detalle y Cobrar"** (antes "Pagar"). Por Pagar conserva "Ver detalle y Pagar". Solo texto (1 línea). `ff63562`.

Archivos tocados (2d): `web-admin/src/App.tsx`, `web-admin/src/styles.css`.
Nota: el proyecto **NO usa Tailwind** (React+Vite con `styles.css` + estilos inline); las peticiones que mencionan Tailwind se implementan con esa convención.

## 2e. CAMBIOS 2026-08-24 (sesión UI + 2 features backend)
26. **Caja — fix menú vertical (bug real):** reglas globales `nav{flex-direction:column}` y `nav button{width:100%}` (del menú lateral) se colaban en `.cajaSubNav` (que es un `<nav>`). Faltaba fijar `flex-direction:row` y `width:auto` → el menú salía vertical pese al CSS. Ahora sí pills horizontales. `e2113bf`. (Antes: `ecd22f2`/`718c85e` habían puesto pills+esmeralda pero sin el fix del `<nav>` global.)
27. **Dashboard (Panel Integral) — layout:** 7 KPIs → **4 tarjetas** [Compras][Ventas][Bancos/Caja][Saldo General] con chips secundarios (Utilidad/margen, Ing. servicio, Costo op.). "Inventario por accionista" a fila propia full-width; "Ventas"/"Compras por accionista" en grid 2-col. Sección final agrupada en panel **"Resumen ejecutivo"** (bancos por socio + cobrar/pagar/préstamos + alertas). Solo layout. `9529690`.
28. **Liquidaciones (2 rondas):** (a) filtro en vivo de "Materia prima por liquidar" por el agricultor elegido (reusa memo `farmerLots`) + `.tableHead span{min-width:0;overflow-wrap:anywhere}`. `4561412`. (b) esa tarjeta pasó de `DataList` a **tabla real** con `<th>` `white-space:nowrap`+padding dentro de `overflow-x:auto`; form de lotes en grid `6fr 3fr 3fr` (Ingreso/QQ/Precio). `8b4f9b2`.
29. **Sacos especiales (BACKEND+FE):** al empacar subproductos se descuentan los sacos especiales sin peso. Mapeo FIJO por producto: Arrocillo→"Saco Usado (Arrocillo)", Polvillo→"Saco Negro (Polvillo)"; Rechazo/otros no. Conteo con **lb/saco variable** (95/96/100…): `sacos=QQ*100/lb`. Nuevos `tipoSacoEspecial()` y `descontarSacosPorTipo()` en `cargo-empaque.ts`; `descontarSacosPorPeso` delega. Aplica en `processing.ts` (subproductos del pilado) y `selection.ts` (por producto). FE: Selección input lb/saco para subproductos; Producción campos lb/saco por subproducto. Sin migración (tipos ya existen). Verificado con ROLLBACK. `f3e8643`.
30. **Fomentos (BACKEND+FE):** botón verde **[+ Nuevo Fomento]** arriba-derecha; se quitó el `↺ Actualizar` de filtros; el form pasó a **modal**; empty state con CTA en la columna derecha. Campos nuevos **Variedad de Grano** y **Límite de Crédito ($)**. Migración `20260826` (columnas `variedad`, `limite_credito`, nullable). **Decisión: límite SOLO informativo** — NO cambia el crédito (`monto_limite` sigue = cuadras*800 en SELECT_FOMENTO). `c320974`.
30a. **Fomentos — amortización LIFO en Liquidaciones (`f53c6a3`):** al liquidar, el descuento de fomento amortiza los fomentos ACTIVOS del agricultor (de CUALQUIER accionista) en orden **LIFO** (`created_at DESC`) en cascada. `amortizarFomentosLIFO()` en `backend/src/services/fomento-liquidacion.ts`. Cada fomento tocado se **cierra** (`status='CERRADO_LIQUIDACION'`, `liquidado_at`); si el pago no cubre todo, el remanente se traspasa a un fomento **nuevo** del mismo socio (renovación). El abono real conserva el **cruce inter-socios** (CxP del que liquida + CxC del dueño); el traspaso NO cruza. Endpoint `GET /liquidations/:batchId/fomento-recibo` (descuentos por accionista + nuevos saldos) alimenta el recibo (`printLiqBatch` → sección "Amortización de Fomentos (LIFO)"). Reversa (anular) reabre los cerrados y borra cruces por `reference_id=fomento_id OR pago_id`. Sin migración (status es text).
30b. **Fomentos — cierre/archivo/impresión/arrastre (`2285d99`):** (1) los cerrados se archivan: filtro **🗄️ ARCHIVADOS** (`status='CERRADO_LIQUIDACION'`), y TODOS/ACTIVOS ya **no** los muestran; badge "🗄️ CERRADO" + banner de archivado en el detalle. (2) botón **[🖨️ Imprimir Estado de Cuenta]** en el detalle (usa `GET /fomentos/:id`; `printEstadoCuentaFomento` en App.tsx): comprobante con detalle de entregas+intereses, abonos (incl. liquidación) y **Saldo Resultante** (SALDADO $0.00 / SALDO EN CONTRA $X). (3) el fomento nuevo de renovación ahora lleva `limite_credito` = deuda arrastrada exacta y nota **"Arrastre de saldo en contra - Fomento anterior #<id_anterior>"**, mismo socio, ACTIVOS/HABILITADO.
30c. **Fomentos — folio + editar + eliminar (`55c05c3`):** (1) campo **folio** (N.º de Página/Libreta) en crear/editar; badge **"📖 Pag: X"** en tarjeta y detalle. Migración `20260920_fomento_folio.sql` (columna `folio TEXT`, aditiva). (2) botón **[✏️ Editar]** en el detalle reusa el MISMO modal (`fomentoEditingId`/`openEditFomento`) → `PATCH /fomentos/:id` parcial, NO toca entregas/pagos. (3) botón **[🗑️ Eliminar Fomento]** → modal rojo de confirmación (no borra directo); `DELETE /fomentos/:id` cascadea entregas y pagos (FK `ON DELETE CASCADE`). `fomentoSchema` acepta `folio` (POST+PATCH); `SELECT_FOMENTO` lo expone vía `f.*`.
31. **Agricultores — directorio:** buscador `🔍 nombre/RUC` + conteo ("86 agricultores"); tabla en `max-height:600 overflow-y:auto` con `<th>` sticky (fondo blanco+sombra); nueva columna **Acciones** con **✏️ Editar** → modal (nombre/cédula/teléfono/accionista) que usa `PUT /farmers/:id` (ya existía). Solo FE. `370c4ac`.
32. **Nómina — 3 pestañas pill unificadas:** [🏭 Planta y Secadoras] (Pilador/Estibador + Secador en grid 2-col) · [👷‍♂️ Cuadrilla de Carga/Descarga] (registro rápido: fecha/actividad=tarifa/cuadrilla/sacos, reusa handlers de Cuadrilla) · [📜 Historial de Pagos]. `nominaView: planta|cuadrilla|historial`. NO se removió la pestaña Cuadrilla del menú (gestión completa sigue ahí). Solo layout. `999f0cd`.

Archivos tocados (2e): `web-admin/src/App.tsx`, `web-admin/src/styles.css`; `backend/src/services/cargo-empaque.ts`, `backend/src/routes/modules/{processing,selection,fomentos}.ts`; `database/migrations/20260826_fomentos_variedad_limite.sql`.
**Aprendizaje CSS:** `.cajaSubNav` (y cualquier `<nav>`) hereda reglas globales `nav{flex-direction:column}`/`nav button{width:100%}` del menú lateral → al hacer menús horizontales hay que fijar `flex-direction:row` + `width:auto`.

## 2f. CAMBIOS 2026-08-25 (esta sesión — UI + varios features backend)
33. **Nómina/Cuadrilla — Anticipo:** campo Anticipo en el registro rápido de Cuadrilla (Subtotal = Cantidad×Tarifa − Anticipo); se quitó la leyenda; se **eliminó la entrada duplicada "Cuadrilla" del sidebar** y se centralizó en `/nomina`. Sin tocar cálculos de tarifas ni APIs de pago. `3075da9`.
34. **Reportes (BACKEND+FE):** `/reports/summary` acepta **`?accionista=<id|all>`** (all=consolidado vía `$N::uuid IS NULL`) y **`?scope=cash|accrued`**. `cash`=movimientos de caja (fuente de verdad intacta); `accrued`=Devengado, suma **ventas a crédito** (AR con `sale_id` pendiente, CREDIT_IN) + **liquidaciones pendientes** (AP con `liquidation_id`, CREDIT_OUT) que aún no tocan caja. Devuelve `breakdown`/`accrued`/`consolidado`/`scope`. FE: selector Accionista (Todos/socio), toggle **Liquidez vs Devengado**, tabla de desglose (Fecha/Concepto/Socio/Tipo/Monto, incl. "Crédito"), export **PDF/Excel** del filtrado, gráfico Ingresos vs Egresos, grid KPIs 2/4. Verificado reversible (AR crédito solo en scope=accrued). `7c2ff16`, `99cb4ad`.
35. **Configuración — rediseño a pestañas verticales (5 grupos):** `configSubTab: "operacion"|"tarifas"|"cuadrilla"|"socios"|"secuenciales"|"usuarios"`. Layout `.configLayout`/`.configVTabs`/`.configVContent` (colapsa en móvil). Botón persistente Guardar + toast. Campos inexistentes → placeholders; secciones extra reubicadas dentro de los 5 grupos. `6c473df`.
    - **Socios & Bancos:** tabla más ancha (`.configSociosGrid`, overflow-x); **cuentas bancarias por socio** — `GET /finance/bank/accounts/all` (admin, join accionistas) + `PUT /finance/bank/accounts/:id` relajado a admin cross-accionista. `1a56099`.
    - **Tarifas y Servicios de Planta** separado de **Cuadrilla** (pestaña propia), grid 2-col (`.configTarifasGrid`), sin barra flotante; tabla de actividades sincronizada con `/nomina`. `36a2541`.
    - **Secuenciales (funcional):** `GET /settings/sequences` (Guía editable + valores reales; Venta/Liquidación último código, solo lectura) + `PUT /settings/sequences/guia` (admin, `setval` + prefijo). Prefijo de Guía configurable: columna `guia_prefix` en `app_settings` (migración `20260826_guia_prefix.sql`); `orders.ts` usa `COALESCE((SELECT guia_prefix...), '001-001-') || LPAD(nextval('guia_remision_seq'),9,'0')`. Modal seguro para reiniciar numeración. `2bcd461`.
    - **Control de Usuarios:** grid 5/7 (`.configUsersGrid`), tablas con overflow-x, links [☑️ Seleccionar todos]/[⬜ Limpiar] por módulo, tabla Actividad con `max-height`+scroll y `thead` sticky. `17fba13`.
36. **Estados Financieros (BACKEND+FE) — 3 tandas:**
    - **(a)** Filtro compacto + botones rápidos **[Hoy][Este Mes][Año Actual]** (`loadFinanzas(desde?,hasta?)` acepta fechas explícitas) + **[📄 Descargar PDF]** (vista imprimible con Balance/Resultados/Indicadores). **Ratios "sin deuda":** con **Pasivo Corriente = 0**, Liquidez corriente y Prueba ácida ya no muestran `0.00 ⚠` sino **N/A / verde (ok)** — `finance.ts` marca `sin_deuda:true, ok:true`; medidor y tabla de indicadores lo respetan. **Activos fijos:** nombres con codificación rota se muestran saneados (`sanitizeEquipName`: `T�nel`→`Túnel`) y botón discreto **[🗑️]** por fila (`DELETE /equipment/:id`). `0301e73`.
    - **(b)** Cabecera de "Activos fijos" con **[🧹 Limpiar duplicados sin costo]** y **[➕ Agregar Activo Fijo]** (alta manual de bienes que NO son maquinaria: vehículos, muebles, oficina → `POST /finance/fixed-assets`, `type` varchar libre). **Conciliación bancaria:** el texto pasivo pasa a CTA **[🏦 Ir a Caja para Aperturar Banco]** (`setActiveTab("Caja")`) cuando no hay cuentas. **Drill-down** del Estado de Resultados: "Mercadería vendida" y "Combustible de secado" son enlaces (`.linkBtn`) que abren un modal con los registros que componen cada cifra (`GET /finance/costo-ventas/detalle`, mismos criterios que el agregado). `dc3bbc1`.
    - **(c)** Limpieza de duplicados como verbo **DELETE** (`DELETE /finance/fixed-assets/duplicados-sin-costo`): borra activos con costo 0/NULL cuyo nombre saneado se repite, dejando 1 por nombre; idempotente, respeta mantenimiento (`{eliminados, protegidos}`). Tabla de activos fijos: por defecto muestra solo **costo>0** (ordenado con costo primero) + checkbox **"Mostrar equipos sin costo ($0.00)"** con conteo. Consultas de Balance/Resultados auditadas: parametrizadas + `SUM` en servidor (no traen filas masivas). `ab2c5ff`.
37. **Báscula — filtro "Pendientes" (BACKEND):** `/tickets?status=pending` solo descartaba liquidados, así que incluía los **Ingresado** (con `weighing_ticket_id`). Ahora `pending = liquidated_at IS NULL AND weighing_ticket_id IS NULL` (alineado con el chip "Pendiente" de la vista). "Todos" y "Liquidados" iguales; contrato/tipos/respuesta sin cambios. Default de la vista ya era "Pendientes". Verificado en vivo: Todos=213 (199 pend + 14 ingresados), Pendientes=199 (0 ingresados/liquidados). `b24bcab`.

Archivos tocados (2f): `web-admin/src/App.tsx`, `web-admin/src/styles.css`; `backend/src/services/finance.ts`; `backend/src/routes/modules/{reports,settings,finance,orders,mobile-tickets}.ts`; `database/migrations/20260826_guia_prefix.sql`.

## 2g. CAMBIOS 2026-08-26/27 (esta sesión)
**En `main` (pusheado):**
1. **Báscula — parámetros de planta + merma + Web Serial** (`bf68bbd`): Config→Operación y Planta ahora edita **Tarifa de pilado ($/QQ)** y **Humedad base (%)** (en `app_settings`, defaults 3.50 / 13.0; migración `20260826_plant_params.sql`; `PUT /settings/plant-params`). En el ingreso manual de báscula: **calculadora de merma en vivo** (Peso Neto Verde lb/QQ, desc. humedad `(H−base)/(100−base)`, desc. impureza, QQ liquidados; 1 QQ=100 lb; kg→lb ×2.2046) — **solo visual, no cambia lo guardado**. **Conector Web Serial** `[🔌 Conectar Báscula]` (navigator.serial @9600) + [Capturar Peso].
2. **Producción — origen flexible + eficiencia** (`7dc8ec3`): radios **Desde Secadoras / Desde Stock** (lote de arroz seco en bodega, reusa la rama lot-only del backend). Tarjeta **📊 Eficiencia del Lote** (%.blanco / %subproductos / %merma, badge >62 óptimo / <60 alto). Guarda anti-doble-conteo: la rama lot-only rechaza lotes ya procesados (409).
3. **Config — acabados UI** (`a04dd6a`): "Vista previa de encabezado" simula papel térmico; empty states elegantes (`CatEmptyState`) en Categorías; asteriscos rojos en labels obligatorios (Nombre comercial, RUC); grid RUC/Teléfono gap 16.
4. **Config › Cuadrilla** (`ab1930c`): grid 5/7 (`.cuadCfgGrid`), buscador en vivo, inputs de tarifa a 2 decimales onBlur, botón 🚫 para ocultar/inactivar actividad (`PUT /cuadrilla/activities/:id {is_active:false}`).
5. **Producción — destino Selección vs Empaque** (`f587730`, IMPORTANTE, toca nómina+inventario): radios **🧺 Selección (Tulas)** / **📦 Empaque (Sacos)**. Empaque muestra presentaciones (10/25/50/98/100 LB + opción ⚙️ Peso Personalizado/Pico) + subproductos; Selección solo Tulas + Peso Total (QQ). Backend `finish-production` acepta `destino`: **SELECCION no descuenta sacos** (tula reutilizable, arroz va a producto terminado disponible para Selección); **EMPAQUE descuenta sacos** exactos por presentación. Nómina (`labor.ts`) reescrita con **XOR estricta del estibador**: con tulas>0 paga SOLO `(tulas/3)×tarifa_3tulas`; sin tulas `qq×tarifa + sacas×tarifa + arrocillo×tarifa`. Pilador inmutable `qq×tarifa + sacas×tarifa`. **CAMBIO DE COMPORTAMIENTO:** el pago por saca ahora usa los **sacos reales consumidos** en empaque (antes llegaba 0 desde el front) → pilador/estibador cobran el componente de sacas. Verificado con ROLLBACK (Selección 6 tulas → Est $10.00; Empaque qq100/sacas4/arrocillo5 → Pil $15.60 / Est $11.50).

**En rama `feat/modulo-caja-campo` (NO pusheada, migración NO aplicada):**
6. **MÓDULO NUEVO E INDEPENDIENTE "Caja de Campo"** (`8bf3678`) — cosechadora, transporte/fletes, caja de campo. Aparte de piladora/ventas/fomentos. V1 = solo captura (CRUD). Tablas con prefijo `campo_` (6): activos, categorias_gasto, cuentas, clientes, servicios, movimientos. Todo **UUID** (convención confirmada: `users.id` es uuid; PG 18 → `gen_random_uuid()` nativo, sin pgcrypto). `created_by` = **FK real a `users(id) ON DELETE SET NULL`**. Derivados NO guardados: saldo de cuenta = entradas−salidas; por servicio cobrado=Σ movimientos entrada ligados, saldo=valor−cobrado, estado=pendiente/abonado/pagado. Regla del valor del servicio: con qq+precio_unitario → valor=qq×precio; si no, manual. Migración validada por dry-run (ROLLBACK, DDL OK) pero **sin aplicar**.

Archivos (2g): `web-admin/src/App.tsx`, `web-admin/src/styles.css`; `backend/src/routes/modules/{settings,processing,labor,campo}.ts`, `backend/src/routes/index.ts`; **nuevos**: `backend/src/routes/modules/campo.ts`, `web-admin/src/campo/CampoModule.tsx`; migraciones `20260826_{plant_params,campo_caja_modulo}.sql`.

## 2h. CAMBIOS 2026-08-27 tarde (V2 Reportes de Campo — rama `feat/campo-reportes`, NO pusheada)
Reportes del módulo Campo. **No crea tablas**; todo sale de `campo_servicios`/`campo_movimientos`. Cálculos **en SQL** (GROUP BY/SUM/FULL JOIN), no en JS.
1. **Fuente única de saldo:** vista **`campo_servicios_saldo`** (migración `20260827_..._view.sql`) expone por servicio `cobrado`/`saldo_pendiente`/`estado`. El endpoint `GET /campo/servicios` y el chequeo de sobrepago **leen de la vista** (ya no recalculan). Commit `97c81fd`.
2. **3 endpoints de reportes:**
   - `GET /campo/reportes/saldo-caja?hasta=` → saldo por cuenta (entradas−salidas) a fecha de corte + total.
   - `GET /campo/reportes/por-cobrar` (siempre **a hoy**, sin corte histórico) → servicios con saldo>0 por cliente (saldo, antigüedad, **tramo** 0-30/31-60/61-90/+90) + **subtotales por tramo** + detalle por servicio + total general.
   - `GET /campo/reportes/por-maquina?mes=|desde=&hasta=` → por activo: ingresos, gastos por categoría, ganancia; gastos **sin `activo_id` van en fila "SIN ASIGNAR"** (cero prorrateo).
3. **Pantalla "📊 Reportes"** (pill nueva dentro del módulo Caja de Campo, NO dashboard aparte): selector período mes/rango; Panel saldo caja (tarjetas+total); Panel por cobrar (subtotales por tramo, tabla por cliente **expandible** al detalle, total); Panel por máquina (ingresos/gastos desglosados/ganancia negativa en rojo, fila SIN ASIGNAR, fila TOTALES). Montos 2 decimales, números a la derecha. Commit `855a85e`.
Verificado en navegador con datos `RPT*`: saldo caja 315; por cobrar 1000 en 4 tramos (450/200/100/250); por máquina cosA 680 / camB 100 / SIN ASIGNAR −15, totales 950/185/765.

Archivos (2h): `backend/src/routes/modules/campo.ts`, `web-admin/src/campo/CampoModule.tsx`; **nuevo**: `database/migrations/20260827_campo_servicios_saldo_view.sql`.

## 2i. CAMBIOS 2026-08-27 noche (Campo: operación de CEYRO, aislado, V3 caja)
**Ya en `main`:**
- **Campo como operación de CEYRO** (`75df164`): el selector de accionista de arriba muestra, bajo CEYRO, `CEYRO · Planta / Matriz` y `CEYRO · Campo`. Elegir "Campo" entra al módulo (activeTab="Caja de Campo"), manteniendo CEYRO activo. Se quitó la pastilla de "Caja de Campo" del sidebar (grupo Operación). "Planta / Matriz" NO es una operación aparte: es el subtítulo del accionista CEYRO. `visibleTabs` admite "Caja de Campo" solo bajo CEYRO (no va en `navGroups`). `switchAccionista` recarga la página (marca en localStorage `campo-pending` para aterrizar en Campo tras reload).
- **Contexto AISLADO** (`c99c23e`): con Campo activo, App.tsx hace early-return de un layout propio `CampoWorkspace` (sidebar con brand + selector de operación + menú propio, sin módulos compartidos). Menú extensible `CAMPO_SECCIONES`. `CampoModule` recibe prop `section`.

**En rama `feat/campo-caja` (V3, `5d822ae`, NO pusheada):**
- **Nombre de operación editable**: tabla `campo_config` (1 fila, `nombre_operacion` default 'Campo'), `GET/PUT /campo/config`. Sección "⚙️ Configuración" en el menú de Campo. App.tsx lee el nombre y lo usa en el selector; `CampoWorkspace` en brand/topbar/footer. Solo la etiqueta de Campo lo usa.
- **Sección Caja** (reusa `campo_movimientos`): menú de Campo reorganizado a **Caja · Servicios · Reportes · Configuración**. Caja = saldos por cuenta + total; ＋Ingreso (opc. ligar a servicio → abono, respeta 422); －Egreso (categoría obligatoria, activo opc.); ⇄Transferencia (par salida+entrada, `naturaleza='transferencia'` + `par_id`, en 1 tx); 📒Libro con **saldo corrido** (window SQL), filtros cuenta/fecha, egresos en rojo.
- **Migración** `20260827_campo_config_transferencias.sql`: `campo_config` + `campo_movimientos.naturaleza` (default 'operativo') + `par_id`.
- **Reportes ajustados**: `por-maquina` EXCLUYE transferencias de gastos (`naturaleza='operativo'`); `saldo-caja` SÍ las refleja por cuenta (total neto correcto); `por-cobrar` sin cambios. Verificado con transferencia: saldo total cuadra, por-maquina sin "SIN ASIGNAR" falso.

Archivos (2i): `web-admin/src/App.tsx`, `web-admin/src/campo/CampoModule.tsx`, `backend/src/routes/modules/campo.ts`; **nuevo**: `database/migrations/20260827_campo_config_transferencias.sql`.

## 2j. CAMBIOS 2026-08-28 (esta sesión)
**Ya en `main` (merge FF de `feat/campo-caja`, commit `00676f4`):** se cerró la **V3 Caja de Campo** — limpieza de datos de prueba `RPT*/RPT2*` + demo (BD local en cero, semillas intactas), `campo_config.nombre_operacion='Campo'`, merge+push, rama borrada.

**En rama `feat/campo-maquinaria` (`1b1e394`, NO pusheada):** **Flota y Maquinaria (Centros de Costo)** para Transporte/Cosecha.
- **Decisión de arquitectura**: en vez de crear tablas paralelas (`maquinaria` + `movimientos_caja.maquinaria_id`) que duplicarían el dominio de Campo, se **extiende** el catálogo existente `campo_activos` (= maquinaria) y la FK ya existente `campo_movimientos.activo_id` (= maquinaria_id). El usuario eligió "Extender Campo (Recomendado)".
- **Migración** `20260828_campo_maquinaria.sql` (aditiva): `campo_activos` + `placa_codigo VARCHAR(40)` y `tipo` ampliado a `cosechadora|camion|vehiculo|transporte|otro` (CHECK `campo_activos_tipo_check` recreado).
- **Backend** (`campo.ts`): GET/POST/PATCH `/campo/activos` con `placa_codigo` + `z.enum(TIPO_MAQUINARIA)`. (El endpoint de edición es **PATCH**, no PUT.) El libro ya devolvía `activo_nombre`.
- **Frontend** (`CampoModule.tsx`): nueva tarjeta **"🚜 Flota y Maquinaria"** en Configuración con CRUD completo (alta/edición/archivar-activar; nombre-alias, tipo, placa/código, operador, estado) — reemplaza el `ActivosPanel` mínimo; Servicios muestra hint a Configuración si no hay flota. Egreso: selector **"Asignar a Máquina / Vehículo (Opcional)"** debajo de Concepto (maquinaria activa). Libro: **badge "🚜 &lt;máquina&gt;"** junto al concepto cuando el egreso está asignado. Helper `patchMaquina` (usa `apiFetch` con method PATCH).
- **Verificado en navegador**: alta/edición(PATCH)/archivar de flota, egreso asignado, badge en libro ("Cambio de aceite 🚜 Cosechadora 1"), 4 tipos. QA data limpiada tras probar.

Archivos (2j): `backend/src/routes/modules/campo.ts`, `web-admin/src/campo/CampoModule.tsx`; **nuevo**: `database/migrations/20260828_campo_maquinaria.sql`.

## 2k. CAMBIOS 2026-08-28 (Egreso reordenado + Vales/Anticipos — `b904b4c`, en main)
Reestructura del egreso de Campo y gestión de fondos por rendir. **Se apoya en Flota/Maquinaria** (el selector de máquina, ahora obligatorio, viene de §2j).
1. **Egreso reordenado (orden estricto)**: Fecha · **Máquina/Vehículo\*** · Categoría\* · Concepto · Monto · Cuenta. La máquina es **obligatoria** con opción por defecto **"🏢 Gastos Generales / Administración"** = `activo_id` nulo. En `por-maquina` la fila nula se renombró de "SIN ASIGNAR" a "Gastos Generales / Administración".
2. **Concepto dinámico por subcategoría**: si la categoría es **REPARACION_MANT**, el Concepto pasa a `<input list>` (datalist, elegir de lista **o** escribir libre) con 20 opciones en 6 subcategorías (Cabezal, Trilla/Limpieza, Motor/Eléctrico, Rodaje/Orugas, Hidráulico/Transmisión, Descarga). Otras categorías = texto libre. Constante `REPARACION_SUBCATS` en `CampoModule.tsx`.
3. **Fondos por rendir / Vales de anticipo**: checkbox en el egreso → `estado='PENDIENTE_RENDICION'`. Nueva sección de menú **📋 Vales** (pendientes/liquidados) con **🧾 Liquidar/Rendir**. Modal de liquidación: al ingresar el **monto real gastado** genera un movimiento de **AJUSTE** (`naturaleza='ajuste_vale'`, hereda cuenta/activo/categoría del vale): **devolución** (entrada) si sobró, **reembolso** (salida) si faltó; marca el vale **LIQUIDADO** y guarda `monto_rendido`.
4. **Reportes**: `por-maquina` GASTOS **netea `ajuste_vale`** (devolución resta, reembolso suma) → el gasto de la máquina queda en el REAL; INGRESOS salen SOLO de `campo_servicios` (el ajuste **nunca** aparece como ingreso). `saldo-caja` cuenta el ajuste (mueve caja real). Transferencias y abonos sin cambios.
5. **Backend**: `es_anticipo` en `POST /movimientos` (solo egreso, si no → 400); **`GET /campo/movimientos/vales?estado=`**; **`POST /campo/movimientos/:id/liquidar`** (lock `FOR UPDATE`, 409 si ya liquidado). Migración `20260828_campo_vales_anticipos.sql`: `+ estado, monto_rendido, vale_id` en `campo_movimientos` + índice de pendientes.
Verificado E2E por API (neto de caja y gasto por-máquina correctos en devolución/reembolso; 400/409 de borde) y en navegador (orden del form, default General, datalist REPARACION_MANT, sección Vales + modal con preview + refresco, chips del libro). BD local limpia tras QA.

Archivos (2k): `backend/src/routes/modules/campo.ts`, `web-admin/src/campo/CampoModule.tsx`; **nuevo**: `database/migrations/20260828_campo_vales_anticipos.sql`.

## 2l. CAMBIOS 2026-08-29 (conceptos estandarizados + Partes Diarios de Cosecha)
Todo en `main`. Cada feature se hizo en rama propia y se cerró con FF+push (flujo habitual). Frontend salvo Partes (que sí toca backend).
1. **Egreso · Concepto de REPARACION_MANT con selects Pieza + Acción** (`6054f86`→`ae08f9d`): reemplaza el datalist por 2 `<select>` (Pieza con `<optgroup>` + Acción) que arman el Concepto como **"Pieza - Acción"** (editable para detalle extra; recambiar un select lo reconstruye). Constantes `ACCIONES_MANT` (4) y `PIEZAS_MANT` en `CampoModule.tsx`: **10 subgrupos / 41 piezas** (4 de cosechadora + 6 de vehículo: Motor y Combustible, Frenos, Suspensión y Dirección, Transmisión, Eléctrico y Climatización, Carrocería y Estructura). Otras categorías: Concepto texto libre.
2. **Ingreso · Concepto estandarizado** (`6a1696c`): el input de Concepto pasa a `<select>` con `<optgroup>` (`CONCEPTOS_INGRESO`: Servicios Agrícolas, Transporte, Otros Ingresos — 7 opciones) + opción "✍️ Escribir concepto manualmente…" + input de detalle extra que concatena **"Base - Detalle"** (o texto libre si no hay base). Sin cambios de backend.
3. **Partes Diarios de Cosecha** (nuevo módulo, `f6b4309` mock → `2a81f75` backend → `91bcc21` cobro): sección **📝 Partes Diarios** en `CAMPO_SECCIONES` (debajo de Vales), componente `web-admin/src/campo/PartesModule.tsx`.
   - Tabla `campo_partes` (`20260829_campo_partes.sql`): fecha, `activo_id` FK→`campo_activos`, operador, cliente (texto), qq, observaciones, `estado` ('por_cobrar'|'cobrado', default por_cobrar), created_by. + `servicio_id` FK→`campo_servicios` (`20260829_campo_partes_cobro.sql`).
   - Endpoints: `GET /campo/partes` (lista con `activo_nombre` + estado/valor/saldo del servicio vía LEFT JOIN `campo_servicios_saldo`), `POST /campo/partes` (crea; 404 máquina, 400 cliente/qq), **`POST /campo/partes/:id/cobrar {precio_unitario}`** (lock; resuelve/crea cliente por nombre; crea `campo_servicio` tipo 'cosecha' valor=qq×precio; marca parte 'cobrado' + enlaza; 409 si ya cobrado).
   - UI: form (Fecha, Máquina=flota real, Operador autollenado del activo/editable, Cliente, QQ, Observaciones) + tabla FECHA·OPERADOR·MÁQUINA·CLIENTE·QQ·OBS·ESTADO. Estado por_cobrar → badge + botón **💵 Generar cobro** (modal precio×QQ con preview); cobrado → badge "Cobro generado" + estado del servicio (pendiente/abonado/pagado + saldo).
4. **Partes · pulido** (`da07a77`, sin migración): **filtros** en el historial (Desde/Hasta, Máquina, Estado) — `GET /campo/partes` acepta `activo_id` y `estado`; **columna Acciones**: 💵 Cobrar · ✏️ Editar (`PATCH /partes/:id`) · 🗑️ Anular (`DELETE /partes/:id`) en partes por-cobrar, y ↩️ Des-cobrar (`POST /partes/:id/descobrar`) en cobrados. **Reglas de integridad contable**: editar/anular SOLO si `estado='por_cobrar'` (409 si ya cobrado → hay que des-cobrar antes); **des-cobrar** revierte 'cobrado'→'por_cobrar' borrando el `campo_servicio`, SOLO si NO tiene abonos (409 si ya cobró algo → primero reversar abonos en Caja). Todo con lock `FOR UPDATE`.
Verificado E2E (API + navegador): conceptos concatenan bien; parte se persiste; cobro genera servicio (valor 150), aparece en /servicios y por-cobrar, abono 60 → abonado/saldo 90; 409 doble cobro; editar/filtros/anular/des-cobrar (incl. 409 al editar/borrar cobrado y al des-cobrar con abonos). BD limpia tras QA.

Archivos (2l): `web-admin/src/campo/{CampoModule,PartesModule}.tsx`, `backend/src/routes/modules/campo.ts`; **nuevos**: `database/migrations/20260829_campo_partes.sql`, `20260829_campo_partes_cobro.sql`.

## 2m. CAMBIOS 2026-08-31 (Operadores + QQ por máquina + tarifa en partes; Ops) — en main
Todo en `main`. Rama propia cerrada con FF+push.
1. **Catálogo de Operadores** (`ab80b4a`): tabla `campo_operadores` (`20260831_campo_operadores.sql`: nombre, identificacion, telefono, activo, created_by) + endpoints `GET/POST/PATCH /campo/operadores` (con `?solo_activos=1`). CRUD **"👷 Operadores"** en Configuración (junto a Flota). En **Partes Diarios** el campo Operador pasó de texto libre a **`<select>`** alimentado por operadores activos (se guarda el NOMBRE en `campo_partes.operador`, sin migrar esa columna; el modal de editar conserva un operador legacy que ya no esté en el catálogo, como `<option>` extra).
2. **QQ acumulado por máquina** (`ab80b4a`): `/reportes/por-maquina` suma `campo_partes.qq` por activo en el rango con un **FULL JOIN** extra (CTE `prod`) — así aparecen incluso máquinas que SOLO tienen partes (sin ingresos/gastos). Nueva columna **"QQ trab."** + total en la tabla del reporte (frontend `Maquina.qq`).
3. **Editar tarifa de partes cobrados sin des-cobrar** (`ab80b4a`): **`PATCH /campo/partes/:id/tarifa { precio_unitario | valor }`** actualiza el `campo_servicio` vinculado (valor = qq×precio, o monto total directo con precio derivado = valor/qq). **Bloquea con `409`** si el servicio ya tiene abonos ("Reverse los abonos en Caja antes de modificar la tarifa"). UI: botón **"✏️ Tarifa"** en partes cobrados (deshabilitado si hay abonos) + modal con toggle precio/QQ ↔ monto total y preview del nuevo valor.
4. **Ops · despliegue automatizado** (`14c8fae`/`25e9052`): `deploy.sh` + `ecosystem.config.js` (PM2, app `bascula-erp`, fork) + `.gitattributes` (LF en `*.sh`). Ver §0.
Verificado E2E (API + navegador): operadores CRUD/filtro; QQ por máquina=200; tarifa 1.5→180 y monto 200→200; **409 al editar tarifa con abono**. BD limpia tras QA.

Archivos (2m): `backend/src/routes/modules/campo.ts`, `web-admin/src/campo/{CampoModule,PartesModule}.tsx`; **nuevos**: `database/migrations/20260831_campo_operadores.sql`, `deploy.sh`, `ecosystem.config.js`, `.gitattributes`.

## 2n. CAMBIOS 2026-08-31 (Estado de Cuenta de Clientes — `23e3f81`) — en main
Nueva sección **👥 Clientes** en el workspace de Campo (menú entre Servicios y Vales). **Alcance SOLO Campo** (aislado del ERP de piladora; el usuario descartó unir con Planta). Migración `20260831_campo_clientes_identificacion.sql`: + `identificacion` en `campo_clientes`.
- **Endpoints**: `GET /campo/clientes/estado-cuenta` (lista: debe=Σ servicios, haber=Σ abonos, saldo histórico real, + `total_pendiente`; filtros `q` nombre/identificación y `estado` al_dia/pendiente — se filtran en JS sobre el agregado SQL). `GET /campo/clientes/:id/estado-cuenta?from&to` (línea de tiempo: servicios cosecha/flete = Debe con máquina+QQ, abonos = Haber con cuenta; **saldo corrido**; `saldo_apertura` = neto previo a `from`; totales). `POST /clientes` acepta `identificacion`; nuevo `PATCH /clientes/:id`. `GET /clientes` busca también por identificación.
- **Frontend** (CampoModule): `ClientesView` (buscador + filtro estado + total por cobrar + tabla con saldo), `EstadoCuentaModal` (rango fecha, timeline Debe/Haber/Saldo, **ficha imprimible/PDF** con QQ/máquina/desglose de abonos vía `window.open`), `EditarClienteModal`.
- **Integridad**: el saldo se DERIVA en vivo (no se almacena) → ajustes de tarifa y des-cobros de Campo se reflejan al recargar. Abonos siguen atados a servicio→cliente; sobrepago con guard 422 intacto.
Verificado E2E (API: lista debe/haber/saldo, filtros, buscador por identificación, timeline con apertura/totales; navegador: sección, lista, modal, imprimir). BD limpia tras QA.

Archivos (2n): `backend/src/routes/modules/campo.ts`, `web-admin/src/campo/CampoModule.tsx`; **nuevo**: `database/migrations/20260831_campo_clientes_identificacion.sql`.

## 2o. CAMBIOS 2026-08-31/09-01 (servicio-desde-parte + sesiones de caja + filtro báscula) — en main (`81a0677`)
Stack de 3 features cerrado con FF.
1. **Servicio desde Parte** (`12d16bf`): en el form "Nuevo servicio" (Campo→Servicios), selector "📋 Seleccionar Parte Diario pendiente" (lista `campo_partes` por_cobrar: Fecha·Cosechadora·Cliente·Operador·QQ). Al elegir: autollena fecha/máquina/QQ, resuelve/crea el cliente (alta rápida) y guarda `parte_id`; el precio lo pone el usuario. `POST /campo/servicios` acepta `parte_id` opcional → transacción con lock: verifica parte `por_cobrar` (409 si no), crea el servicio y marca el parte `cobrado`+enlazado (misma semántica que `/partes/:id/cobrar`). Sin `parte_id`: registro manual habitual.
2. **Apertura/Arqueo/Cierre de Caja** (`23ac5b4`): tabla `campo_caja_sesiones` (`20260901_campo_caja_sesiones.sql`; una sola ABIERTA por índice único parcial). **Bloqueo**: `POST /movimientos` y `/transferencias` que toquen la cuenta **CAJA** exigen sesión ABIERTA → 400 "Debe abrir caja antes de registrar movimientos" (BANCO/OTROS libres; helper `requireCajaAbierta`). Endpoints: `/caja/sesion-activa` (abierta + arqueo en vivo + saldo sugerido=último cierre), `/caja/abrir` (409 si ya hay), `/caja/cierre-preview` (saldo_inicial+ingresos−egresos de CAJA desde apertura=teórico), `/caja/cerrar` (diferencia; opcional ajuste `naturaleza='ajuste_caja'` en el libro), `/caja/sesiones` (historial). UI en la sección Caja: barra 🔴/🟢, modales de apertura y arqueo (diferencia Cuadrado/Faltante/Sobrante en vivo), pill "📋 Cierres de Caja".
3. **Báscula · Corregir accionista** (`81a0677`): `GET /weighing-tickets/materia-prima` filtra por el accionista activo (`req.accionistaId`): MATRIZ/global = todos; socio = los suyos O sin asignar (oculta los de otro socio). Frontend: el efecto de recarga de tickets depende también de `activeAccionistaId` (reactivo al cambiar de socio).
Verificado E2E (API + navegador). BD: LIBERATO es dato REAL del usuario — ver §8.

Archivos (2o): `backend/src/routes/modules/{campo,weighing-tickets}.ts`, `web-admin/src/{App.tsx,campo/CampoModule.tsx}`; **nuevo**: `database/migrations/20260901_campo_caja_sesiones.sql`.

## 2p. CAMBIOS 2026-08-31 (CICLO DE FLETES Planta↔Campo — todo en main `e1b7396`)
Cierre completo del flujo de fletes internos. 4 features, todas verificadas E2E (rollback + HTTP + navegador) y cerradas con FF a main. La máquina local ya corre este código (migraciones aplicadas).
1. **Nuevo Cliente en Campo** (`2a33957`): botón "＋ Nuevo Cliente" + modal (Nombre req · Identificación · Teléfono · tipo) en la vista Clientes. Migración `20260902_campo_clientes_telefono.sql` (+`telefono`).
2. **Integración Báscula→Campo** (`1c5b374`): al INGRESAR materia prima de un SOCIO con un **vehículo de la Flota Propia** (match de la placa del ticket contra `campo_activos`), un disparador NO bloqueante crea un **Parte Diario** en Campo (`origen='bascula'`, `por_cobrar`, operador NULL, referencia en observaciones). Servicio compartido `backend/src/services/campo-flete-bascula.ts` (`dispararFleteInternoBascula` + `crearParteDesdeBascula`), enganchado en `mobile-tickets` create-lot (fire-and-forget, nunca frena la romana). Endpoint manual `POST /campo/partes/integracion-bascula`. Mapea socio→cliente `piladora` por NOMBRE (idempotente por referencia). Migración `20260902_campo_partes_origen.sql`. UI: badge azul **"⚖️ Báscula"** en Partes Diarios.
3. **Cruce de fletes en Liquidación** (`296a2f7`): en la liquidación de la piladora, el flete se captura **POR INGRESO** (monto + tipo Propia/Tercero + vehículo), **auto-detectado** desde la placa del ticket (`pending-entries` resuelve placa→`campo_activos` vía LATERAL) y editable. **Flota Propia** → el flete descontado cruza la deuda interna: abona el/los `campo_servicios` por cobrar del cliente-piladora (FIFO, prefiere mismo vehículo) con una ENTRADA en la cuenta interna nueva **`CRUCE PILADORA`** (NO CAJA/BANCO); lo que no empareje queda como **crédito a favor**. **Tercero** → solo informativo (sin asiento). UI: fila de flete por línea + desglose "Flota Propia (interno) / Terceros (a pagar)". Servicio `backend/src/services/campo-cruce-flete.ts` (`cruzarFleteInterno`, corre dentro del tx de la liquidación). Migración `20260903_campo_cuenta_cruce_piladora.sql` (semilla cuenta).
4. **Conciliación crédito-a-favor** (`e1b7396`): en Clientes de Campo, banner + botón **"⚡ Convertir parte y aplicar crédito"** por cliente-piladora con crédito. `GET /campo/conciliacion/creditos` (crédito disponible + partes pendientes) y `POST /campo/conciliacion/convertir-y-aplicar` (tx: crea servicio flete desde el parte, marca parte `cobrado`, aplica crédito con **doble asiento** en CRUCE PILADORA: salida que consume el crédito + entrada/abono que salda el servicio; nunca supera crédito ni valor). Migración `20260904_campo_mov_cliente_credito.sql` (+`campo_movimientos.cliente_id` para atribuir el crédito). Fix hallado: `FILTER` mal ubicado tras `COALESCE` (SQL 500) → dentro del `SUM(...)`.

Archivos (2p): `backend/src/routes/modules/{campo,liquidations,mobile-tickets,weighing-tickets}.ts`, `backend/src/services/{campo-flete-bascula,campo-cruce-flete}.ts` (nuevos), `web-admin/src/{App.tsx,types.ts,styles.css,campo/{CampoModule,PartesModule}.tsx}`; migraciones nuevas `20260902_campo_clientes_telefono.sql`, `20260902_campo_partes_origen.sql`, `20260903_campo_cuenta_cruce_piladora.sql`, `20260904_campo_mov_cliente_credito.sql`.

## 2q. CAMBIOS 2026-09-02 (Homologación + Pago automático de Cuadrilla por túnel — todo en main)
6 iteraciones, todas verificadas (rollback + HTTP + navegador) y cerradas con FF a main. Commits: `12c6850` (homologación), `f220b26` (pago auto cuadrilla + sync corrida), `c40e1ef` (secador por corrida), `4264463` (ciclo completo + automatización 100%).
1. **Homologación de agricultores + API externa** (`12c6850`): tabla `agricultor_alias` (alias→agricultor_id). El import nativo de báscula (`importBasculaTickets` en `mobile-tickets.ts`) ahora resuelve el nombre **exact match en farmers → alias → null** (Pendiente), **ya NO auto-crea agricultores**. `POST /tickets/:id/link-farmer` acepta `guardar_alias` (guarda el nombre externo como alias). Frontend Báscula: tickets con `farmer_id=null` en rojo "⚠️ Cliente Desconocido" + botón "Vincular cliente" con autocompletado. **API externa** `GET /api/v1/external/agricultores` (router `external.ts`, montado antes de `requireAuth`) protegida por **API Key** en header `x-api-key` (env `EXTERNAL_API_KEY` o autogenerada en `backend/.external-api-key`, gitignored). Migración `20260909_agricultor_alias.sql`.
2. **Pago automático de cuadrilla desde Secadoras + sync de corrida** (`f220b26`): tabla `drying_tunnel_cuadrilla` (asignación cuadrilla↔túnel, con `momento` LLENADO/VACIADO y QQ); `cuadrilla_activities.categoria` (labores de túnel = 'SECADORA'); `cuadrilla_entries` gana `origen`/`referencia_id`/`tunnel_number`/`momento`. **Sincronización de corrida por motor**: los túneles de un mismo motor **heredan obligatoriamente** `filled_at` + `dry_start_at` de la corrida (evita desfases de día). Datos de corrida (fecha/hora/secador) únicos a nivel motor en el form; endpoint `POST /process-flow/drying/motor/:motor/sync-run` aplica a todos los túneles activos. Migraciones `20260910_campo…`→`20260910_cuadrilla_secadora.sql`, `20260911_cuadrilla_tunel_split.sql`.
3. **Secador por corrida (no por día)** (`c40e1ef`): `/labor/secador-suggestions` agrupa por `filled_at` (fecha oficial de la corrida) en vez de `dry_start_at::date`; una corrida que cruza la medianoche = **una sola guardianía**. Expone `dias_corrida`.
4. **Ciclo completo + automatización 100%** (`4264463`): (a) detección también del **VACIADO** (Botada/Sacado, túnel con `dry_end_at`); labor VACIADO = "BOTADA DE TUNEL", LLENADO = "RECEPCION A TUNEL N". Migración `20260912_cuadrilla_vaciado.sql` (`drying_tunnel_cuadrilla.momento` + índice único por trabajador). (b) **Vista unificada** "Registros del período": manuales + automáticos (🔒🤖, inmutables); **editar/eliminar solo manuales** (`PUT /cuadrilla/entries/:id`, bloquea origen SECADORA con 409). (c) **Maestro `cuadrillas`** (`20260913_cuadrillas_master.sql`) + creación rápida (`POST /cuadrilla/groups`, pop-up); `/cuadrilla/workers` une entries+asignaciones+maestro. (d) **Se removió TODO el bloque de cuadrilla de la vista de Secadoras** (redundante). (e) **Automatización 100%**: `POST /cuadrilla/tunnel-autoprocess` — detecta todos los eventos nuevos del rango, asigna la cuadrilla activa y **genera los pagos directo** a la tabla, idempotente. El botón "🔍 Detectar labores de túnel" ya no muestra cola de pendientes: si hay 1 cuadrilla la usa directa; si 0 abre "Crear Cuadrilla"; si varias pide elegir una para el lote.

Archivos (2q): `backend/src/routes/modules/{mobile-tickets,external,cuadrilla,process-flow,labor,index}.ts`, `backend/src/config/env.ts`; `web-admin/src/{App.tsx,api.ts}` (`apiPatch` añadido); migraciones `20260909`…`20260913`. **Endpoints muertos** creados y luego reemplazados por el flujo auto: `/cuadrilla/tunnel-suggestions` (detección detallada, aún existe) y `/cuadrilla/tunnel-generate` (aún existe). El PATCH `/process-flow/drying/:id/cuadrilla` (asignar+procesar un evento) sigue existiendo pero el frontend ya no lo llama (usa autoprocess).

## 2r. CAMBIOS 2026-09-03 (8 features, todas verificadas E2E + navegador y cerradas con FF a main)
Commits en orden: `7447ad6` (empaque túnel), `1391c19` (MIX pilado), `f54988a` (sacos centralizados matriz), `895c830` (sacos solo-matriz UI/API), `570a15e` (Caja↔sacos), `d9dbb54` (precio base sacos), `fda8bc7` (snapshot rendimiento), `f79221e` (**Gana**). Migraciones `20260914`…`20260917`.
1. **Empaque Tulas/Sacos por túnel** (`7447ad6`, `20260914_tunnel_empaque.sql`): `drying_tunnel_reports` gana `recepcion_empaque`/`recepcion_sacos` + `botada_empaque`/`botada_sacos` (default `TULAS`). En Secadoras (crear = informe motor; editar/finalizar) hay selector "Tipo de Empaque" por momento. En **Nómina→Cuadrilla "Detectar labores de túnel"** (`tunnel-autoprocess`), la labor/tarifa depende del empaque: **Tulas** → `RECEPCION A TUNEL N`/`BOTADA DE TUNEL` por QQ; **Sacos** → `ENSACADO` (o `SACADO EN SACO`) por Nº de sacos. Helper `resolveTunnelLabor` en `cuadrilla.ts`. NO altera pesos ni tickets.
2. **MIX Tula/Saco por línea en Pilado** (`1391c19`): se quitó el radio GLOBAL Selección/Empaque; cada renglón del reporte de pilado tiene **Destino/Empaque** (🧺 Tula→Selección / 📦 Saco 100/50/… LB). `white_rice_presentations[]` gana `destino`/`tulas`. Inventario: todo el arroz blanco va a producto terminado; **solo las líneas en Saco descuentan sacos** (las de Tula no). **Nómina (sin cuadrilla)**: se rompió la XOR del estibador → en un lote MIXTO cobra la parte en tulas (por Nº tulas) **+** la parte en saco (QQ/sacas/arrocillo); Pilador por todo el QQ. `createProductionWorkerPayments` acepta `qqSaco`. Retrocompatible.
3. **Sacos = inventario EXCLUSIVO de la Matriz** (`f54988a`+`895c830`): `sack_inventory` es tabla única (sin accionista_id) → siempre es de la matriz. (a) El consumo por pilado registra en el kardex `sack_movements` la nota "Consumo de empaque por pilado de Lote [código] - Socio: [nombre]" + `ref_batch` (helpers `descontarSacosPor{Peso,Tipo}` aceptan `refBatch`). Se eliminó el egreso legacy per-socio de `/:id/finish`. (b) En **Inventario**, el bloque "📦 Inventario de Sacos" se **oculta a socios** (`esCeyroActivo`). (c) Guard `assertMatriz` en las **escrituras** de sacos (`POST /sacks/movements`, `PATCH /sacks/:id/adjust`, `POST /sacks/purchases`) → 403 si el accionista activo no es MATRIZ. Lecturas abiertas.
4. **Caja↔Inventario de Sacos** (`570a15e`, `20260915_*.sql`): categoría de caja **COMPRA_SACOS** ("Compra de sacos", EGRESO/Matriz, sembrada). Al registrar ese egreso con detalle (tipo+cantidad), el backend genera **ENTRADA automática** en sacos (concepto "Ingreso automático por compra registrada en Caja") enlazada por la **nueva col `sack_movements.ref_cash_movement`** (FK a `cash_movements`, ON DELETE SET NULL). **Anular el egreso** (`/cash/movements/:id/reverse`) genera **SALIDA** que revierte el inventario (idempotente por neto). Aplica también al carrito `/sacks/purchases` (ahora enlaza su ENTRADA al egreso). Helpers `registrarEntradaSacosDesdeCaja`/`reversarEntradaSacosDeCaja` en `cash.ts`.
5. **Precio base de sacos** (`d9dbb54`, `20260916_*.sql`): `sack_inventory.precio_compra_default` (100LB 0.35, 25LB 0.25, 10LB 0.20, Negro 0.26, Usado 0.15…). En Caja→Compra de Sacos, al cambiar el tipo se **autocompleta** el "Precio unitario" (editable). En Inventario de Sacos (Matriz), cada tarjeta tiene input "Precio base $" que guarda onBlur (`PATCH /sacks/:id/precio`, matriz).
6. **Cuadro de rendimiento por lote (snapshot)** (`fda8bc7`, `20260917_*.sql`): al cerrar el pilado se congela `production_yields.rendimiento_snapshot` (JSONB): cáscara entrada, arroz blanco total + split Tulas/Sacos + presentaciones, subproductos con lb/saco, y porcentajes (blanco, excedente=(blanco−entrada)/entrada, por subproducto, total, merma) + accionista. `/processing-batches/history` lo expone.
7. **Módulo «Gana»** (`f79221e`, solo frontend): nuevo tab en el Sidebar OPERACIÓN **entre Producción e Inventario**. Muestra los cuadros de rendimiento de los lotes finalizados **filtrados por accionista** (multi-socio). Fórmulas de **división directa** sobre la cáscara de entrada (= suma de secadoras del lote): %Blanco=blanco/cáscara; %Arrocillos=(3/4+Fino)/cáscara (agrupados); %Polvillo=polvillo/cáscara. Se **quitó de Producción** la tarjeta verde de eficiencia, el panel post-cierre y el historial con cuadros; **al Finalizar Lote redirige a «Gana»**. Reusa `/processing-batches/history` (ya per-accionista). Nota: `millingEficiencia` memo + `productionHistoryOpen` quedaron sin uso (dead code inofensivo).

## 2s. CAMBIOS 2026-09-04 (Gana → liquidación FINANCIERA de pilado)
Commits `7dcb86d` (base financiera) + `3519c8a` (precio por producto). Migraciones `20260918`, `20260919`. Todo en main `3519c8a`.
1. **Costo · Cáscara desglosada por partida**: se eliminó la fila única; ahora una sub-fila por secadora/ticket (agricultor · ticket · QQ **calificado** · **precio de compra** · costo=QQ×precio). El precio de compra se **cruza desde Liquidaciones** (`liquidations.price_per_quintal` por `weighing_ticket_id`, última no CANCELLED); partida sin liquidar → "— pend.". Fuente de partidas: `processing_batch_drying_lots` (por batch).
2. **CORRIGE el total de cáscara** (bug 269.18): antes `input_paddy_kg/45.359` (peso BRUTO en kg); ahora **SUMA los QQ calificados de las partidas** (ej. real 122.10). Los % de rendimiento usan el total corregido. `entrada = Σ partidas.quintals` (fallback snapshot/input si no hay partidas — lotes viejos).
3. **Ingreso · precio de venta por CADA producto de salida** (Arroz Blanco, Arrocillo 3/4, Arrocillo Fino, Polvillo): input editable por producto; **Total($)=QQ×precio**; fila **INGRESO TOTAL PROYECTADO** = Σ. Guardado en `production_yields.precio_venta_blanco/broken/fine/bran` vía **`PATCH /processing-batches/:id/precio-venta {producto, precio}`** (whitelist de columnas `PRECIO_VENTA_COL`, aislado por accionista → 403 si es de otro). En la tabla los arrocillos van en **2 filas separadas** (3/4 y Fino) para precio individual. `/history` expone `entradas[]` + los 4 precios.
4. **Separación Costos↔Ingresos + Utilidad**: sección superior Cáscara = costos; inferior Productos = ingresos; resumen `Utilidad proyectada = Ingreso Total − Costo cáscara`. Columnas nuevas de la tabla: **Precio Unit.** y **Total ($)** (sin perder los %).
Verificado E2E sobre lote real de ROVINSON (`LT-20260902182415`, 3 partidas de Liberato): total 122.10 (no 269.18), cruce precio compra $15.50→$651, ingreso por producto (135×40+20×20+4×12+100×6=6448), aislamiento STALYN→403, datos reales restaurados a null.

## 3. REGLAS DE NEGOCIO (no romper)
- **Toma de pedido NO mueve dinero ni inventario**; recién al **Despachar** sale stock + entra caja (Contado) o Cuenta por Cobrar (Crédito). Estados DB: `PENDING`/`DELIVERED`/`CANCELLED` (NO renombrar; hay CHECK). El pedido genera su CxC "(pendiente de despacho)" al tomarse; al despachar se salda o se enlaza, nunca se duplica.
- **Cuentas espejo entre accionistas**: un servicio/cargo de la matriz a un socio crea CxC (CEYRO) + CxP (socio) enlazadas en tablas puente (`pilado_services`, `lot_transfers`, `matriz_service_charges`, `matriz_packaging_charges`). Un abono en una cara debe reflejarse en la otra + caja del otro socio → `backend/src/services/cuentas-vinculadas.ts` (`espejarAbonoEnContraparte`). Saldar una cuenta debe mover caja + espejar, no solo bajar saldo.
- **"PENDIENTE"** en cuentas = `document_status='CONFIRMED'` con `balance=amount`. El enum `document_status` (DRAFT/CONFIRMED/PARTIAL/PAID/CANCELLED) **NO tiene 'PENDING'** — no inventarlo.
- **Cargo por empaque**: solo si el vendedor es SOCIO (la matriz no se cobra a sí misma); agrupa sacos 10/25/50 lb × tarifa; si total>0 crea CxC/CxP. Atómico dentro del despacho.
- **Inventario de SACOS = solo de la MATRIZ (CEYRO)**: `sack_inventory` es tabla única (sin accionista_id). El saco se consume al EMPACAR (cierre de Producción/Selección), NO en la venta. Siempre se descuenta de `sack_inventory` (la matriz) via `descontarSacosPorPeso()`, sin importar el accionista del lote. En la VENTA no se toca el stock de sacos: solo baja el producto terminado del socio + (si aplica) el CARGO financiero por empaque (CxC CEYRO / CxP socio). Son dos cosas distintas: consumo físico (empaque) vs deuda financiera (venta).
- **Sacos ESPECIALES sin peso (subproductos):** al empacar, Arrocillo(*)→"Saco Usado (Arrocillo)" y Polvillo/Afrecho→"Saco Negro (Polvillo)" (mapeo fijo por producto en `tipoSacoEspecial()`); Rechazo/otros no descuentan. Nº sacos = QQ*100/(lb por saco), donde lb es variable por cliente (95/96/100…). Descuento vía `descontarSacosPorTipo()`. El arroz blanco sigue por peso ("Saco N LB") con `descontarSacosPorPeso` (que ahora delega en el anterior).
- **Combustible secadoras**: por MOTOR y por corrida (`filled_at`), repartido entre las 2 secadoras aunque terminen distinto.
- **Nómina**: estibador cobra por tulas (proporcional 5/3); guardianía del secador es UNA por **corrida** (no por día calendario ni por secadora) — la corrida se identifica por `filled_at` unificado del motor; una corrida que cruza medianoche = 1 guardianía.
- **Corrida por motor**: los túneles del mismo motor iniciados la misma jornada COMPARTEN `filled_at` + `dry_start_at` (unificados). El backend lo fuerza al crear un túnel si ya hay otro activo del motor (hereda del ancla). No romper: de esto dependen la nómina del secador y el pago de cuadrilla (fecha oficial de la corrida).
- **Pago de cuadrilla por túnel (Recepción/Botada)**: la cuadrilla opera como **una sola entidad** (se paga el 100% del QQ del túnel al grupo; NO hay división por trabajador). Fuente de la asignación = tabla `drying_tunnel_cuadrilla` (por `momento` LLENADO/VACIADO). El pago se genera en `cuadrilla_entries` con `origen='SECADORA'` = **inmutable en Nómina** (no editar/borrar; 409 con mensaje "…edite el túnel directamente"). Cálculo = `total_quintals × tarifa` (LLENADO→"RECEPCION A TUNEL N", VACIADO→"BOTADA DE TUNEL"). VACIADO solo aplica a túneles finalizados (`dry_end_at` set). El flujo es **detectar→auto-registrar** (`tunnel-autoprocess`), NO se asigna en la vista de Secadoras (ese bloque se eliminó). "La única cuadrilla activa" = lista `/cuadrilla/workers` de longitud 1.
- **Homologación de agricultores (báscula externa)**: los tickets llegan de una app EXTERNA con typos. El import resuelve el nombre: **exact match en `farmers` → `agricultor_alias` → `farmer_id=null`** ("Pendiente de Vincular"). **NUNCA auto-crear agricultores** desde el import. Vincular un ticket puede guardar el nombre externo como alias. La app externa consume `GET /api/v1/external/agricultores` (API Key) para armar su propio autocomplete.
- **Categorías de mantenimiento**: soft-delete (nunca borrar filas); inactivas salen de forms pero se conservan en histórico/reportes; `area`/`section`/`maintenance_type` se guardan como TEXTO en `equipment_maintenance` (retrocompat).
- **Costos/consultas**: agrupaciones son a nivel query o frontend; NO borrar ni alterar registros base de deudas.
- **Báscula — estado "Pendiente"** = ticket sin procesar: `liquidated_at IS NULL AND weighing_ticket_id IS NULL` (ni Ingresado como materia prima ni Liquidado). El filtro `pending` del backend refleja exactamente esto; el chip de la vista se deriva igual (en_espera → "En espera 2º pesaje", weighing_ticket_id → "Ingresado", liquidated_at → "Liquidado", si no → "Pendiente"). "Todos" muestra el historial completo.
- **Indicadores financieros sin deuda**: si Pasivo Corriente = 0, Liquidez corriente y Prueba ácida NO aplican (división por 0) → se marcan `sin_deuda:true, ok:true` (N/A verde), no `0.00 ⚠`.
- **Reportes — alcance (scope)**: `cash` = movimientos de caja (fuente de verdad, sin alterar). `accrued` = Devengado, AÑADE ventas a crédito (AR pendiente con sale_id) y liquidaciones pendientes (AP con liquidation_id) que aún no tocan caja. `accionista=all` consolida (sin filtrar por accionista). No mezclar: el desglose etiqueta "Crédito" lo devengado.
- **Activos fijos**: viven en la tabla `equipment` (`acquisition_cost`/`acquisition_date`/`useful_life_years`/`salvage_value`/`is_depreciable`). `type` es varchar libre (PILADORA/SECADORA/MOTOR/OTRO + categorías manuales VEHICULO/MUEBLES…). Alta manual vía `POST /finance/fixed-assets`; depreciación lineal calculada en `getActivosFijos`. Los de costo 0 no entran al balance (solo se listan para cargarles el costo).
- **Producción — destino del arroz pilado (nuevo)**: **Selección (tulas)** → arroz a producto terminado SIN descontar sacos (tula reutilizable), `finish-production` con `destino:"SELECCION"`, sin presentaciones/subproductos; nómina estibador = SOLO `(tulas/3)×tarifa_3tulas`. **Empaque (sacos)** → `destino:"EMPAQUE"`, presentaciones descuentan sacos exactos; nómina estibador = `qq×tarifa+sacas×tarifa+arrocillo×tarifa`. Pilador SIEMPRE `qq×tarifa+sacas×tarifa`. El pago por saca usa los sacos REALES consumidos (backend), no lo que mande el front.
- **Merma en báscula (nuevo)**: humedad base configurable (`app_settings.humedad_base_pct`, default 13). Desc. humedad = `(H−base)/(100−base)`; 1 QQ = 100 lb; kg→lb ×2.2046. Es **solo cálculo visual** en el ingreso; NO cambia los QQ que se guardan (el ticket conserva su modelo de calificación).
- **Módulo Caja de Campo (independiente)**: aparte del ERP de piladora — NO comparte tablas ni lógica con túneles/piladora/ventas/fomentos. Tablas `campo_*` (UUID; `created_by` FK a `users`). **Fuente única de saldo = vista `campo_servicios_saldo`** (cobrado/saldo/estado); endpoint de servicios, chequeo de sobrepago y reportes leen de ahí (no recalcular en otro lado). Derivados: saldo cuenta=entradas−salidas; por servicio cobrado=Σ movimientos entrada con ese `servicio_id`, saldo=valor−cobrado, estado=pendiente/abonado/pagado. Valor del servicio: qq+precio→calculado, si no manual. Sobrepago bloqueado (422) con lock. Reportes: cálculo SQL, gastos sin `activo_id` → "SIN ASIGNAR" (cero prorrateo). Router `/campo` (no está en `WRITE_MODULES_BY_PREFIX` → escrituras a cualquier autenticado; admin sin límite). Es una **operación de CEYRO** (selector de arriba), con **contexto aislado** (layout propio, menú `CAMPO_SECCIONES`: Caja·Servicios·Reportes·Configuración). Nombre de la operación **editable** en `campo_config` (solo la etiqueta de Campo lo usa).
- **Campo — transferencias**: una transferencia entre cuentas = PAR de movimientos (salida origen + entrada destino), `naturaleza='transferencia'` + `par_id` común, en 1 transacción. Regla clave para no descuadrar reportes: las transferencias **NO cuentan como ingreso/egreso real** (los reportes de gasto las excluyen con `naturaleza='operativo'`), pero **SÍ mueven el saldo de cada cuenta** (saldo-caja las incluye; el total neto no cambia porque salida y entrada se cancelan). Los movimientos normales son `naturaleza='operativo'`.
- **Campo — egreso**: orden estricto de campos (Fecha·Máquina*·Categoría*·Concepto·Monto·Cuenta). La **máquina es obligatoria** con default "🏢 Gastos Generales / Administración" = `activo_id` nulo (esos gastos van a la fila general, antes "SIN ASIGNAR"). Si la categoría es `REPARACION_MANT`, el Concepto es un datalist con opciones por subcategoría (lista o texto libre); otras categorías = texto libre.
- **Campo — vales / anticipos (fondos por rendir)**: un egreso con checkbox de anticipo se guarda `estado='PENDIENTE_RENDICION'` (sigue siendo `operativo`, cuenta como gasto comprometido de su máquina). Al **liquidar** (rendir) se ingresa lo REAL gastado y se genera un movimiento **`naturaleza='ajuste_vale'`** (hereda cuenta/activo/categoría del vale): devolución=entrada si sobró, reembolso=salida si faltó; el vale pasa a `LIQUIDADO` con `monto_rendido`. Regla clave: **`ajuste_vale` NO es ingreso real** — los ingresos de `por-maquina` salen SOLO de `campo_servicios`; en GASTOS de `por-maquina` el ajuste **se netea** (devolución resta, reembolso suma) → el gasto de la máquina queda en el REAL; `saldo-caja` sí lo cuenta (mueve caja). Todo bajo lock `FOR UPDATE` (409 si ya liquidado; 400 si el anticipo no es egreso).
- **Campo — conceptos estandarizados**: EGRESO `REPARACION_MANT` → Concepto se arma con selects Pieza (`PIEZAS_MANT`) + Acción (`ACCIONES_MANT`) = "Pieza - Acción", editable; otras categorías texto libre. INGRESO → Concepto = select `CONCEPTOS_INGRESO` (o manual) + detalle extra = "Base - Detalle". Son solo ayudas de captura; el backend recibe `concepto` como string opcional (no validar contra las listas).
- **Campo — Partes Diarios de Cosecha**: registro diario por máquina (tabla `campo_partes`), independiente de los movimientos de caja. Un parte nace `estado='por_cobrar'`. **Generar cobro** (`POST /partes/:id/cobrar`) crea UN `campo_servicio` tipo 'cosecha' (valor=qq×precio, cliente resuelto/creado desde el texto del parte) y marca el parte `cobrado` enlazándolo por `servicio_id`; NO duplica contabilidad (el cobro real = abonos sobre ese servicio, vía Servicios/Por Cobrar). Bloqueado con lock (409 si ya tiene cobro). El parte NO mueve caja por sí mismo; solo su servicio lo hace al abonarse. También se puede crear el servicio DESDE el form "Nuevo servicio" con `parte_id` (mismo efecto: liquida el parte). **Integridad**: editar (`PATCH`) y anular (`DELETE`) un parte solo si está `por_cobrar` (409 si ya cobrado); **des-cobrar** (`POST /partes/:id/descobrar`) revierte a `por_cobrar` borrando el servicio SOLO si el servicio no tiene abonos (409 si ya cobró algo → reversar abonos primero). **Editar tarifa** de un cobrado sin des-cobrar (`PATCH /partes/:id/tarifa`) actualiza el servicio vinculado, pero también **409 si el servicio ya tiene abonos**. Filtros del historial: `activo_id`, `estado`, `from`/`to`. El Operador del parte sale del catálogo `campo_operadores` (se guarda el nombre). El reporte por-máquina suma los QQ de los partes del rango ("QQ trab.").
- **Campo — sesión de caja**: la cuenta **CAJA** requiere una sesión ABIERTA (`campo_caja_sesiones`, una sola a la vez) para registrar ingresos/egresos y transferencias que la toquen (400 si no hay). BANCO/OTROS no se bloquean. El **saldo teórico** del arqueo = saldo_inicial + ingresos − egresos de CAJA desde `fecha_apertura` (por `created_at`). El cierre guarda diferencia (real − teórico); si `generar_ajuste`, crea un movimiento `naturaleza='ajuste_caja'` en CAJA (excluido de reportes por-máquina; SÍ afecta saldo-caja). La barra de estado, modales y el historial viven en la sección Caja.
- **Báscula — "Corregir accionista"**: `GET /weighing-tickets/materia-prima` se filtra por el accionista activo (header): MATRIZ = todos los ingresos pendientes; socio = los suyos O sin asignar (oculta los de otro socio). No romper: depende de `accionistas.tipo='MATRIZ'` para la vista global.
- **CICLO DE FLETES Planta↔Campo** (crítico, toca dinero real): (1) **Origen automático** — un ingreso de materia prima de un SOCIO con vehículo de la Flota Propia crea un Parte Diario en Campo (`origen='bascula'`). El disparador es fire-and-forget: NUNCA debe frenar ni fallar la romana. Mapeo socio→cliente `piladora` **por nombre** (deben coincidir; ROVINSON/STALYN ya existen como piladora). (2) **Cruce en liquidación** — el flete de Flota Propia descontado al agricultor NO entra a caja: es la piladora saldando su deuda interna con Campo. Se asienta en la cuenta interna **`CRUCE PILADORA`** (nunca CAJA/BANCO). El flete de Tercero es solo informativo (no genera asiento). El neto pagado al agricultor se reduce igual en ambos casos (el flete siempre descuenta). (3) **Crédito a favor** = flete de Flota Propia que no encontró servicio que emparejar. Se guarda atribuido al cliente (`campo_movimientos.cliente_id`, `servicio_id NULL`) en CRUCE PILADORA. Crédito disponible = Σ(entradas−salidas) de ese cliente en CRUCE con `servicio_id NULL`. (4) **Aplicar crédito** (conciliación) = doble asiento en CRUCE: **salida** que consume el crédito (`servicio_id NULL`) + **entrada/abono** que salda el servicio (`servicio_id` set). Nunca supera crédito disponible ni valor del servicio. El **estado de cuenta del cliente** solo cuenta como `haber` los movimientos CON `servicio_id` → la salida de consumo NO lo distorsiona. No cambiar la definición del crédito ni el doble asiento sin re-verificar el estado de cuenta.

## 4. PROBLEMAS PENDIENTES
- ~~Tarifas de empaque en $0.00~~ **RESUELTO**: 10/25/50 = 0.20/0.22/0.27, editables en UI.
- ~~Verificación en navegador~~ **HECHA 2026-08-21**: Ventas, Por Cobrar, Por Pagar, Inventario y filtro de KPIs verificados en vivo. (Nota: el panel del navegador debe estar VISIBLE para clics/capturas; si está oculto, usar `read_page`/`get_page_text`.)
- `web-admin/src/App.tsx` es enorme (~12.7k líneas) — sin modularizar (deuda técnica, no urgente).
- ~~Guía de Remisión: RUC/dirección en blanco~~ **RESUELTO 2026-08-22**: `customers` ya se capturan/editan (form + modal ✏️ Editar en Ventas→Clientes) y la Guía los imprime. Falta que el usuario **cargue los datos reales** de sus clientes desde la UI (hoy solo existe NANCY VELEZ, sin RUC/dirección).
- ~~Sacos especiales sin peso no se descuentan~~ **RESUELTO 2026-08-24** (`f3e8643`): se descuentan al empacar en Producción y Selección, con lb/saco variable. Ver regla en §3. **Probado 2026-08-25:** Selección end-to-end vía ruta real (cierre arrocillo @96lb → "Saco Usado" 200→198 + SALIDA; reversible, base intacta). Producción: rama de subproductos ejecutada contra BD real (labels ARROCILLO_34+FINO→Saco Usado −4, POLVILLO_AFRECHO→Saco Negro −4, con lb/saco variable) vía ROLLBACK (sin residuo). Ambos sacos especiales OK.
- Fomentos: los campos nuevos `variedad`/`limite_credito` se GUARDAN pero **aún no se muestran** en el detalle/lista del fomento (solo en el form). Agregar si se quiere verlos.
- Estados Financieros: `sanitizeEquipName` solo **cosmético** (repara `T�nel`→`Túnel` en la vista). Los nombres rotos siguen en BD; se limpian de verdad con **[🧹 Limpiar duplicados sin costo]** (verificado en vivo: quedó 1 "Motor Túnel 3"). Si quedan equipos rotos con costo>0, editarlos manualmente.
- Nómina «Planta y Secadoras»: la tabla Pilador/Estibador (11 columnas) queda en media columna con scroll horizontal. Si molesta, pasar esa tabla a ancho completo y el Secador debajo.
- `POST /customers/quick` crea con `customer_type='RETAIL'` (no NATURAL/EMPRESA); el tipo del front es una unión de 2 valores → al editar un cliente RETAIL el select cae a "Natural". Menor; alinear si molesta.
- Nota de pruebas en vivo: la sesión del navegador in-app se limpia al reabrir el preview; para pruebas por API se puede generar un JWT admin con `signToken` (secreto en `backend/src/config/env.ts`) y llamar `http://localhost:4000/api/v1/...`. La extensión "Claude en Chrome" no está conectada (no se puede manejar el Chrome real del usuario).
- ~~CAMPO V3 — sin mergear~~ **RESUELTO 2026-08-28**: mergeado a main (`00676f4`), datos de prueba limpiados, `campo_config='Campo'`, rama borrada.
- ~~CAMPO Flota/Maquinaria — sin mergear~~ **RESUELTO 2026-08-28**: mergeado a main (`1b1e394`), rama borrada.
- ~~CAMPO Egreso reordenado + Vales — sin mergear~~ **RESUELTO 2026-08-28**: mergeado a main (`b904b4c`), QA limpio, rama borrada.
- **CAMPO — corte histórico en por-cobrar**: hoy es "a hoy". Un corte a fecha pasada necesitaría filtrar los abonos por fecha (otra consulta); se dejó para después si hace falta.
- **CAMPO Partes Diarios**: ~~filtros / editar / anular / des-cobrar~~ **HECHO** (`da07a77`). ~~catálogo de operadores~~ **HECHO** (`ab80b4a`: `campo_operadores` + select). ~~QQ del parte en reporte por-máquina~~ **HECHO** (`ab80b4a`: columna "QQ trab."). ~~editar precio del cobro sin des-cobrar~~ **HECHO** (`ab80b4a`: `PATCH /partes/:id/tarifa`, 409 si hay abonos). Sin pendientes abiertos del módulo Partes.
- **CAMPO Vales — anticipo pendiente cuenta como gasto comprometido**: mientras el vale NO está liquidado, aparece en `por-maquina` como gasto por su monto entregado (a propósito, es plata que ya salió). Al rendir, el `ajuste_vale` lo netea al real. Si se quisiera EXCLUIR el pendiente del gasto hasta rendir, habría que darle una naturaleza propia y filtrarla (no pedido).
- **Libro de movimientos — saldo corrido**: es acumulado por orden de fecha sobre el conjunto filtrado por cuenta; sin filtro de cuenta es el saldo combinado de todas. No arranca de un "saldo de apertura" del rango (empieza desde el inicio de los movimientos de esa cuenta). Suficiente por ahora.
- **Producción destino (a validar por el usuario)**: el pago **por saca** del pilador ahora SÍ suma (antes 0). Si el negocio quería pilador solo por QQ, hay que dejar `sacas=0` en empaque. Confirmar.
- **Fletes — configuración para que dispare automático**: cada socio necesita un cliente `piladora` con el MISMO nombre (ROVINSON ya lo tiene; falta STALYN si aplica) y cada vehículo de la flota su `placa_codigo` igual a la que llega en el ticket de báscula. Sin eso, el disparador crea el parte pero deja el crédito sin atribuir (caso borde) o no dispara.
- **Fletes — conversión de partes en el cruce (no implementado, por decisión)**: el cruce en liquidación solo empareja `campo_servicios` YA existentes. Si el parte del báscula aún no se convirtió a servicio, el flete queda como **crédito a favor** (no se pierde) y se aplica luego con el botón ⚡ de conciliación. Si se quisiera que el cruce convierta partes automáticamente en el momento, es una iteración futura (habría que resolver la tarifa del parte).
- **Fletes — LIBERATO/datos reales**: quedó una duda de sesión previa sobre si un abono real a LIBERATO se perdió en una limpieza QA antigua (saldo $259.97 / 0 abonos, que también es normal en un cobro fresco). Sin confirmar; el usuario debía verificar.
- **Cuadrilla túnel — botadas reales pendientes**: los túneles reales T1/T2 (del cliente "CUADRILLA") tienen **VACIADO/Botada pendiente** en la semana actual. NO se procesaron (es un pago real que decide el usuario). Mañana el usuario puede darle "Detectar labores de túnel" para registrarlas.
- **Cuadrilla túnel — auto-registro en onload NO implementado** (por decisión): el cliente lo puso como opción; generar pagos financieros sin un clic explícito es riesgoso, así que el registro es solo por el botón. Activarlo si el usuario lo confirma.
- **Cuadrilla túnel — tarifas del catálogo**: si no existe la actividad "RECEPCION A TUNEL N" o "BOTADA DE TUNEL" (activa), ese evento se **omite** en autoprocess (se reporta en `sin_tarifa`). Configurar tarifas en Cuadrilla→Actividades si falta alguna.

## 5. PRÓXIMO PASO
**No hay trabajo a medias.** Todo mergeado y pusheado a main (`3519c8a`, incl. Gana financiera §2s + las 8 features de 2026-09-03 §2r); ramas borradas; datos de prueba ZZ limpiados (0 residuos, 0 huérfanas); dato real de ROVINSON restaurado (precios de venta a null). **La máquina local ya corre este código con migraciones aplicadas** (`20260914`…`20260919`). **Solo si se despliega en OTRO servidor:** `git pull` → `./deploy.sh` (migrate + build BE/FE + reinicio; ver §0). Sin auto-deploy.
Primero mañana: **preguntar al usuario qué sigue.** Pendientes/candidatos: (a) **DEADLOCK LATENTE de `ensureLaborTables`** — se lanzó una tarea aparte para arreglarlo (ver §8); confirmar si se cerró. (b) ¿restaurar la **tarjeta verde de eficiencia en vivo** en Producción? (se quitó al mover rendimiento a Gana; si la querían durante la captura, se repone). (c) botadas reales pendientes de T1/T2 ("🔍 Detectar labores de túnel"); (d) entregar la API Key externa de báscula (`backend/.external-api-key`); (e) probar el ciclo de fletes en vivo (§2p); (f) PDF/export de reportes; (g) mostrar `variedad`/`limite_credito` en fomentos.

## 6. ARCHIVOS IMPORTANTES
- `web-admin/src/App.tsx` — TODO el frontend (tabs por `activeTab === "..."`; Config por `configSubTab`; Caja por `cajaSubTab`).
- `backend/src/routes/modules/`: `orders.ts` (pedido/despacho/guía; JOIN trae `customer_identification`/`customer_address`), `customers.ts` (CRUD clientes: GET lista con address, POST, `PUT /:id`, quick), `receivable.ts` (CxC), `cash.ts` (payables/pagos), `equipment.ts` (mantenimiento + categorías), `settings.ts` (tarifas empaque + config), `cobros.ts` (matriz→socio), `sacks.ts` (inventario + compra múltiple de sacos).
- `backend/src/services/`: `cargo-empaque.ts`, `cuentas-vinculadas.ts` (espejo).
- `backend/src/auth/require-auth.ts` — permisos por accionista (`WRITE_MODULES_BY_PREFIX`).
- `backend/src/routes/modules/dashboard.ts` — `/dashboard/panel` (KPIs + `per_accionista` + `*_por_acc`); el filtro de las 7 tarjetas se hace en el frontend (`PanelIntegral`).
- `backend/src/routes/modules/`: `processing.ts` (cierre de pilado + descuento de sacos por empaque, incl. especiales), `selection.ts` (selección + descuento por producto), `fomentos.ts` (CRUD fomentos; POST/PUT aceptan `variedad`/`limite_credito`; `monto_limite` computado = cuadras*800), `farmers.ts` (`PUT /:id` edita agricultor, merge parcial).
- `backend/src/routes/modules/finance.ts` — Estados Financieros: `/dashboard`, `/balance`, `/income-statement`, `/fixed-assets` (GET/PUT), **`POST /fixed-assets`** (alta manual), **`DELETE /fixed-assets/duplicados-sin-costo`** (limpieza), **`GET /costo-ventas/detalle`** (drill-down), `/bank/accounts` + `/bank/accounts/all` (admin). Lógica de cálculo en `backend/src/services/finance.ts` (`getBalanceGeneral`/`getEstadoResultados`/`getActivosFijos`/`getIndicadores`/`getCostoVentasDetalle`; ratios `sin_deuda` cuando pasivo=0).
- `backend/src/routes/modules/reports.ts` — `/summary?accionista=<id|all>&scope=cash|accrued` (devuelve `breakdown`/`accrued`/`consolidado`/`scope`).
- `backend/src/routes/modules/mobile-tickets.ts` — tickets de báscula (`/tickets`). Filtro `status`: pending = `liquidated_at IS NULL AND weighing_ticket_id IS NULL`; liquidated = `liquidated_at IS NOT NULL`; sin status = todos. También link-farmer, create-lot (ingreso materia prima), liquidate, import/sync.
- `backend/src/routes/modules/settings.ts` — tarifas empaque + config + **`GET/PUT /settings/sequences`** (Guía editable, `guia_prefix` en `app_settings`).
- `web-admin/src/styles.css` — clases del front. OJO: `nav{flex-direction:column}` global (menú lateral) afecta a cualquier `<nav>` (ver §2e). `.cajaSubNav` = pills horizontales (activa esmeralda #059669). Config: `.configLayout`/`.configVTabs`/`.configVContent`/`.configSociosGrid`/`.configTarifasGrid`/`.configUsersGrid`. `.reportKpiGrid` (2→4). `.linkBtn` (enlace discreto para drill-down).
- `backend/src/routes/modules/labor.ts` — nómina de pilada (`createProductionWorkerPayments`): pilador `qq×tarifa+sacas×tarifa`; estibador XOR (con tulas→`(tulas/3)×tarifa_3tulas`; sin tulas→`qq+sacas+arrocillo`). Tarifas en `labor_rates` (fila única). `processing.ts finish-production` acepta `destino`. **`/labor/secador-suggestions`** = días/corridas del secador (guardianía+túneles), agrupa por `filled_at` (corrida, no día); `/labor/secador-days` genera los pagos SECADOR.
- **CUADRILLA (túnel) — `backend/src/routes/modules/cuadrilla.ts`** (router `/cuadrilla`): actividades (`?categoria=SECADORA`), `/workers` (une entries+asignaciones+maestro `cuadrillas`), `/groups` (crear cuadrilla), `/entries` (GET lista con origen/momento/tunnel; POST manual; **PUT edita solo manual**; DELETE bloquea SECADORA), `/summary`, `/advances`; **túnel**: `/tunnel-suggestions` (detección detallada, aún existe), `/tunnel-generate` (genera desde asignaciones, aún existe), **`/tunnel-autoprocess`** (EL flujo actual: detecta+asigna+genera todo). Helper exportado `upsertCuadrillaSecadoraEntry` (idempotente por `(referencia_id,momento,worker)` índice único parcial). El pago automático LLENADO/VACIADO se dispara desde `process-flow.ts` (createDryingReport / updateDryingReport crean la asignación; PATCH `/process-flow/drying/:id/cuadrilla` asigna+procesa un evento — ya no usado por el front) y desde `tunnel-autoprocess`. Migraciones `20260910_cuadrilla_secadora.sql`, `20260911_cuadrilla_tunel_split.sql`, `20260912_cuadrilla_vaciado.sql`, `20260913_cuadrillas_master.sql`. Frontend: la vista Nómina→Cuadrilla vive en `App.tsx` (sub-tab "registro": panel "🏗️ Recepción y Botada a Túnel" con botón Detectar + "Registros del período" unificada; `detectarYRegistrar`).
- **HOMOLOGACIÓN báscula — `backend/src/routes/modules/mobile-tickets.ts`** (`resolveFarmerHomologado`: exact→alias→null; `importBasculaTickets`; link-farmer con `guardar_alias`) + **`external.ts`** (router `/external`, API Key `requireApiKey`, `GET /external/agricultores`). Env: `backend/src/config/env.ts` (`externalApiKey`, autogenera `backend/.external-api-key`). Migración `20260909_agricultor_alias.sql`. Frontend Báscula (tickets "Cliente Desconocido" + modal Vincular) en `App.tsx`.
- **MÓDULO CAMPO** — `backend/src/routes/modules/campo.ts` (router `/campo`): catálogos (activos[flota]/categorias-gasto/cuentas[saldo]/clientes); servicios (leen vista, sobrepago 422 con lock); movimientos (+`es_anticipo`); **vales** `/movimientos/vales` + `/movimientos/:id/liquidar`; **config** `/config`; **transferencias** `/transferencias`; **libro** `/caja/libro`; **sesión de caja** `/caja/{sesion-activa,abrir,cierre-preview,cerrar,sesiones}`; **clientes/estado-cuenta** (`/clientes/estado-cuenta`, `/clientes/:id/estado-cuenta`); **partes** `/partes` (GET/POST) + `/partes/:id/{cobrar,tarifa,descobrar}` + `PATCH/DELETE /partes/:id` + `/partes/integracion-bascula`; **conciliación** `/conciliacion/creditos` + `/conciliacion/convertir-y-aplicar`; **reportes** `/reportes/{saldo-caja,por-cobrar,por-maquina}`. **Servicios de fletes** (nuevos): `backend/src/services/campo-flete-bascula.ts` (disparador Báscula→parte) y `backend/src/services/campo-cruce-flete.ts` (`cruzarFleteInterno`, corre en el tx de la liquidación). El cruce se invoca desde `liquidations.ts` (POST `/`, campo `flete_detalle` por línea; `pending-entries` resuelve placa→flota vía LATERAL). Frontend `web-admin/src/campo/`: `CampoModule.tsx` (`CampoModule` por `section`: caja/servicios/**clientes**/vales/partes/reportes/config; `CampoWorkspace` layout aislado con `CAMPO_SECCIONES`; `ReportesView`; `ClientesView`+`EstadoCuentaModal`+`ConvertirAplicarModal` (conciliación); sesión de caja + modales apertura/arqueo; forms Ingreso/Egreso/Transferencia/Libro/Config; `ValesPanel`+modal; constantes `PIEZAS_MANT`/`ACCIONES_MANT`/`CONCEPTOS_INGRESO`) y **`PartesModule.tsx`** (Partes Diarios: form + tabla + "Generar cobro" + badge ⚖️ Báscula). El flete por línea en liquidación vive en `App.tsx` (`LiqLine` + `submitLiquidations`). Enganche en `routes/index.ts` + App.tsx (early-return `CampoWorkspace`; selector `operationSelectorEl`; estado `campoNombre`).
- `database/migrations/` — últimas aplicadas en main: `20260826_{guia_prefix,plant_params,campo_caja_modulo}.sql`, `20260827_campo_servicios_saldo_view.sql`. En rama `feat/campo-caja` (sin mergear, aplicada en local): `20260827_campo_config_transferencias.sql`.
- Memoria del asistente: `C:\Users\Usuario\.claude\projects\C--Users-Usuario-...-BASCULA-ERP\memory\` (MEMORY.md + notas).

## 7. DECISIONES TOMADAS (no volver a preguntar)
- Reutilizar flujos existentes en vez de reconstruir (evita doble contabilidad).
- Catálogos dinámicos (categorías caja/mantenimiento, tarifas) viven en BD; se editan desde el propio front.
- "Pendiente" = CONFIRMED+balance (no tocar el enum).
- Impresión = `window.open` con HTML limpio (equivale a @media print, patrón del ERP).
- Trabajar/commitear en `main` (no en el worktree).
- Modal de cuentas unificado (`CuentaDetalleModal`) para Cobrar y Pagar.
- Guía de Remisión y KPIs del dashboard: filtrar/derivar en el FRONTEND cuando el payload ya trae los datos por accionista (no duplicar en backend). Nº de guía vía secuencia `guia_remision_seq` (formato `001-001-…`).
- Servicio de pilado y Costo Operativo son de la MATRIZ; un socio los ve en $0.
- Compras "carrito" (sacos): frontend acumula en estado local y envía `items[]`; backend hace 1 solo egreso consolidado en la misma transacción. Endpoint retrocompatible con el formato de 1 ítem.
- Patrón de limpieza de vistas: mover config/herramientas de uso no diario a modales (botón secundario en la cabecera) o a Configuración; dejar la vista principal enfocada en su tarea central. Las tarifas globales viven en Configuración→Tarifas.
- En Báscula se dejó a propósito la "Corrección puntual de accionista de materia prima" (no se pidió moverla). Si se quiere quitar, avisar.
- Jerarquía de botones finales (patrón UI): acción secundaria = outline azul (`background:transparent; border:1.5px solid #2563eb; color:#2563eb`); acción principal/definitiva = verde sólido (`var(--c-success)`). En fila, centrados, con `max-width` (no 100%). Aplicado en Secadoras y Producción.
- Datos fiscales de clientes (RUC/CI + dirección) se gestionan en **Ventas→Clientes** (alta + ✏️ Editar); la Guía los consume vía JOIN en `/orders` (no se duplican en el pedido). No se añadió pantalla nueva ni ruta separada.
- **El backend se despliega compilado** (`npm run build` + reinicio del proceso), no con watch. Tras cualquier cambio de backend hay que rebuild+restart para que surta efecto (ver §0/§8).
- **NO usar Tailwind**: el proyecto no lo tiene instalado. El estilo se hace con `web-admin/src/styles.css` (clases) + estilos inline en `App.tsx`. Si una petición pide "usar Tailwind", implementar el mismo resultado con esa convención (no introducir la dependencia).
- Patrón de selector de subvistas tipo **pills horizontales** (una fila, `overflow-x:auto`, activa verde + blanco, inactiva tenue): aplicado en Caja y Nómina (`.cajaSubNav`). Al ponerlo en un `<nav>`, fijar `flex-direction:row` + `width:auto` en los botones (por el `nav{}` global). Reutilizable en vistas con menú interno alto.
- **Sacos especiales:** el "Límite de Crédito" del fomento es SOLO informativo (no cambia el crédito). Los sacos especiales se mapean fijo por producto (Arrocillo→Usado, Polvillo→Negro) y se cuentan con lb/saco variable. Confirmado por el usuario.
- **Nómina unificada:** 3 pills en `/nomina` (Planta y Secadoras · Cuadrilla · Historial). NO se removió la pestaña Cuadrilla del menú (mantiene resumen/anticipos/actividades); la de Nómina es solo registro rápido.
- **Edición por modal** (no en el form izquierdo) para clientes, agricultores y fomentos: los forms de alta usan inputs no controlados → editar en modal con inputs controlados es más limpio.
- Copy por naturaleza del módulo: en cuentas por cobrar el CTA es "Cobrar"; en por pagar, "Pagar".
- **Reportes — fuente de verdad intacta:** el scope `cash` sigue leyendo los movimientos de caja sin alterar cálculos; `accrued` (Devengado) solo AÑADE créditos/pendientes en una capa aparte (no reescribe caja). Export PDF/Excel es del desglose ya filtrado en el front.
- **Configuración — 5 pestañas verticales** (Operación · Tarifas · Cuadrilla · Socios&Bancos · Secuenciales · Usuarios). Campos que no existen aún → placeholders; secciones sueltas reubicadas dentro de esos grupos (no crear pestañas nuevas sin pedirlo). Secuenciales: solo Guía es editable (secuencia real + prefijo); Venta/Liquidación son código-timestamp de solo lectura.
- **Estados Financieros:** activos fijos manuales entran por `equipment` (no tabla nueva); `type` libre. Limpieza de duplicados = verbo DELETE, idempotente, conserva 1 por nombre y respeta mantenimiento. Ratios con pasivo 0 → "sin deuda" (N/A verde), no 0.00. Drill-down del costo de ventas reusa los mismos criterios del agregado (no recalcula distinto). Nombres rotos se sanean solo en la vista.
- **Báscula "Pendientes":** el filtro excluye Ingresado y Liquidado (definición de pendiente en §3). Fix en backend (una cláusula WHERE), sin tocar contrato/tipos ni la vista.
- **IDs = UUID en TODO el esquema** (confirmado 2026-08-27: `users.id` uuid, PG 18.4 → `gen_random_uuid()` nativo, sin pgcrypto). Tablas nuevas usan UUID; `created_by` = FK real a `users(id) ON DELETE SET NULL`.
- **Módulo Campo es INDEPENDIENTE**: prefijo `campo_`, no toca tablas existentes, se trabaja en rama propia, migración se muestra ANTES de aplicar. Parámetros de planta (tarifa pilado / humedad base) viven en `app_settings` (`PUT /settings/plant-params`, admin).
- **Campo — reportes:** todo el cálculo en SQL (GROUP BY/SUM/FULL JOIN), no en JS. Fuente única del saldo = vista `campo_servicios_saldo` (no duplicar la fórmula). Gastos sin `activo_id` = fila "SIN ASIGNAR", nunca prorrateo. Por-cobrar es "a hoy" con tramos 0-30/31-60/61-90/+90. Pantalla de reportes es una pill dentro del módulo (NO dashboard aparte).
- **CI/deploy:** `.github/workflows/ci.yml` es solo CI (typecheck+tests+build+lint, sin BD). NO hay auto-deploy (Render/Railway/etc.). Un push a main NO corre migraciones en producción; el despliegue a prod es manual.
- **Campo — punto de entrada y aislamiento:** Campo es una OPERACIÓN de CEYRO en el selector de arriba (no pastilla de sidebar). Al activarla, App.tsx hace early-return del layout aislado `CampoWorkspace` (nada del ERP estándar). "Planta / Matriz" es solo el subtítulo del accionista CEYRO, no una operación registrada. Ampliar el menú de Campo = una entrada en `CAMPO_SECCIONES` + su caso en `CampoModule` (prop `section`).
- **Campo — nombre editable:** vive en `campo_config` (1 fila). App.tsx lo carga en `campoNombre` y lo baja al selector + `CampoWorkspace` (brand/topbar/footer). SOLO la etiqueta de Campo lo usa; el nombre de las demás operaciones (accionistas) no se toca.
- **Campo — transferencias:** par de movimientos con `naturaleza='transferencia'`+`par_id`; NO son ingreso/egreso real (reportes de gasto filtran `naturaleza='operativo'`), SÍ mueven saldo de cuenta. No cambiar esto sin re-verificar los 3 reportes con una transferencia.
- **Producción destino/nómina**: `labor.ts` estibador es XOR estricta por `tulas`; el pago por saca usa sacos reales. No revertir a `sacas=0` ni volver a sumar arrocillo cuando hay tulas sin confirmarlo con el usuario.
- **Fletes — cuenta interna `CRUCE PILADORA`**: el cruce y la conciliación NO usan CAJA/BANCO (no es efectivo, es compensación interna Planta↔Campo). Cuenta sembrada por migración; los asientos van solo ahí. No enrutar el cruce por CAJA (rompería el arqueo y exigiría sesión abierta).
- **Fletes — captura por línea + auto-detección (elección del usuario "1 y 3")**: el flete se captura por ingreso con selector Propia/Tercero editable, auto-prellenado desde la placa del ticket. Terceros = solo informativo (sin asiento); crear CxP a choferes quedó descartado por ahora.
- **Fletes — crédito atribuido por `cliente_id`**: se añadió `campo_movimientos.cliente_id` para poder detectar/aplicar el crédito por cliente-piladora. El crédito disponible se DERIVA (no se almacena saldo). Aplicar = doble asiento (salida consume + entrada abona); no “editar” el movimiento de crédito original.
- **Fletes — el cruce corre DENTRO del tx de la liquidación**: si algo del cruce falla, la liquidación revierte junto (integridad). El disparador de Báscula, en cambio, es fire-and-forget aparte (no debe afectar la romana).
- **Cuadrilla es UNA sola entidad** (grupo): se paga el 100% del túnel al grupo; NO hay división por trabajador (el cliente lo descartó). El flujo final es **detectar→auto-registrar** con la única cuadrilla; ya NO se asigna cuadrilla en la vista de Secadoras (bloque eliminado a pedido). Los pagos de túnel son inmutables en Nómina (origen SECADORA); se corrigen editando el túnel.
- **Import de báscula NO auto-crea agricultores** (decisión): nombre desconocido → ticket Pendiente (`farmer_id=null`). Se homologa con `agricultor_alias` o se vincula a mano.
- **API externa por API Key** (no sesión): `/api/v1/external/*` va antes de `requireAuth`; la key vive en `backend/.external-api-key` (autogenerada, gitignored). No exponer datos sensibles por ahí (solo catálogo de agricultores activos).

## 8. ADVERTENCIAS (revisar dependencias antes de tocar)
- `cuentas-vinculadas.ts` (espejo CxC↔CxP): tocarlo mal desincroniza libros y cajas entre socios.
- `orders.ts /:id/deliver`: transacción atómica (venta + cargo financiero por empaque + saldo de CxC). YA NO descuenta sacos físicos (eso pasó a Producción/Selección). No romper el orden.
- `sack_inventory` es tabla ÚNICA (de la matriz), sin accionista_id: NO agregarle accionista_id sin revisar `descontarSacosPorPeso()` (usado en processing.ts/selection.ts) y la compra de sacos, que asumen inventario global.
- Enum `document_status`: no agregar 'PENDING' ni cambiar valores.
- Estados de `sales_orders` (PENDING/DELIVERED/CANCELLED): CHECK constraint, no renombrar a español.
- `equipment_maintenance`: guarda textos, no FKs a categorías — no convertir a FK sin migrar histórico.
- Migraciones: siempre aditivas; nunca DROP/TRUNCATE de tablas con datos.
- **Backend compilado:** un cambio en `backend/src` NO tiene efecto hasta `npm run build` + reiniciar el proceso de :4000. Si "no se ve el cambio", verifica primero que el proceso corre el `dist/` nuevo (compara hora de `dist/server.js` y reinicia). No asumir hot-reload.
- `customers.identification` es UNIQUE: el `PUT /customers/:id` captura la violación 23505 y responde 409; no quitar ese try/catch sin manejar el duplicado.
- `mobile-tickets.ts` filtro `status`: la definición de "pending" (`liquidated_at IS NULL AND weighing_ticket_id IS NULL`) debe seguir alineada con el chip de estado del frontend (App.tsx, vista Báscula). Si cambia una, cambiar la otra.
- `finance.ts` (`services`): las agregaciones del Balance/Resultados son `SUM` parametrizadas server-side; NO traerse filas al cliente para sumar. `getBalanceGeneral` llama internamente a `getEstadoResultados` (para la utilidad del ejercicio) → cuidado con recomputar de más. Los ratios con pasivo 0 dependen de `sin_deuda`; no revertir a `div()` crudo.
- `DELETE /finance/fixed-assets/duplicados-sin-costo`: borra de `equipment`. Solo toca costo 0/NULL duplicados y respeta mantenimiento; no ampliar a costo>0 sin confirmar (borraría bienes reales del balance).
- `processing.ts finish-production` + `labor.ts`: transacción de cierre = inventario (PROCESS_OUTPUT) + sacos (según destino) + nómina + servicio de pilado. Delicada: cambiar el destino, los sacos o la XOR de nómina desalinea pagos/stock. Verificar con ROLLBACK antes.
- **Campo — vista `campo_servicios_saldo` es la fuente única del saldo**: servicios, sobrepago y reportes leen de ahí. Si cambia la fórmula de saldo/estado, se cambia en la VISTA (migración), no en cada endpoint. El sobrepago bloquea con lock (`FOR UPDATE` sobre `campo_servicios`) + lee `saldo_pendiente` de la vista.
- **Campo — `campo_activos` es ahora el catálogo de maquinaria** (Flota): no crear tablas paralelas `maquinaria`/`movimientos_caja`. La FK máquina↔egreso es `campo_movimientos.activo_id`. El endpoint de edición de activos es **PATCH** `/campo/activos/:id` (no PUT).
- **Campo — netting de `ajuste_vale` en `por-maquina`**: el reporte por-máquina cuenta como gasto las salidas `operativo` y **netea** los movimientos `ajuste_vale` (entrada resta, salida suma). Depende de que NINGÚN movimiento normal ponga `activo_id` en una entrada (los abonos usan `servicio_id`, no `activo_id`). Si algún flujo nuevo crea entradas con `activo_id` que NO sean rendiciones, revisar el CTE `gas` y el `desglose` de `/reportes/por-maquina` para no restar de más. La liquidación de vales usa lock `FOR UPDATE` sobre el vale.
- **⚠️ Campo tiene DATOS REALES del usuario** (usa el módulo en vivo; p.ej. cliente **LIBERATO** con parte→servicio de $259.97). **NUNCA** hacer `DELETE FROM campo_*` sin `WHERE` acotado en QA: borrar solo filas de prueba por nombre/prefijo reconocible (QA*, UI *, EC*, etc.). Ver memoria `campo-tiene-datos-reales`.
- **Campo — Partes / cobro**: `campo_partes.servicio_id` → `campo_servicios`, y `campo_servicios` es referenciado por `campo_movimientos` (abonos). Para borrar datos de prueba respetar el orden FK: `campo_movimientos` (del servicio/cliente) → `campo_partes` → `campo_servicios` → `campo_activos` → `campo_clientes` → `accionistas`. (Un parte referencia su servicio: borra el parte ANTES que el servicio.) `POST /partes/:id/cobrar` es idempotente por el lock+check de estado (no genera 2 servicios). Un parte 'cobrado' NO se puede recobrar (conciliación incluida: 409).
- **Fletes — crédito/CRUCE PILADORA**: el crédito disponible depende de `campo_movimientos.cliente_id` + `servicio_id NULL` en esa cuenta. Si cambias cómo se estampan esos campos (en `campo-cruce-flete.ts` o en la conciliación), re-verifica: (a) crédito disponible en `/conciliacion/creditos`, (b) que la salida de consumo NO cuente como `haber` en el estado de cuenta (solo cuentan movimientos con `servicio_id`), (c) el doble asiento no descuadre el saldo de CRUCE. La query de crédito usa `SUM(...) FILTER (WHERE ...)` — el `FILTER` va DENTRO del `SUM`, no tras `COALESCE` (fue un bug 500).
- **Fletes — disparador Báscula**: `dispararFleteInternoBascula` se llama fire-and-forget en `mobile-tickets` create-lot (`void ...`, try/catch interno). NO envolverlo en el tx del ingreso ni hacer que su fallo propague: la romana no debe frenarse. El mapeo socio→cliente piladora es por nombre exacto; si no hay cliente piladora, omite (log) sin romper.
- **Cuadrilla `tunnel-autoprocess` procesa TODOS los eventos pendientes del rango** con la cuadrilla dada. En QA/pruebas: usar túneles ZZ con `filled_at` FUERA del rango real (p.ej. 2026-09-05) para no asignar túneles reales por error. `upsertCuadrillaSecadoraEntry` dedup por índice único parcial `(referencia_id,momento,lower(worker))` — no cambiar sin ajustar el `ON CONFLICT`.
- **`drying_tunnel_cuadrilla`** referencia `drying_tunnel_reports` (ON DELETE CASCADE) y `cuadrilla_activities`. Borrar túneles de prueba: `cuadrilla_entries` (por `referencia_id`) → `lots` (cascada a drying_tunnel_reports/_cuadrilla) → `lot_process_reports`.
- **`process-flow.ts` createDryingReport/updateDryingReport**: además de la lógica de secado, insertan la asignación de cuadrilla (LLENADO/VACIADO) si el body trae `cuadrilla_worker`+`cuadrilla_labor_id` — hoy el front NO los manda (el bloque de Secadoras se quitó), así que queda inactivo pero presente. No romper la transacción del secado al tocarlo.
- **`mobile-tickets.ts` — homologación**: `resolveFarmerHomologado` (exact→alias→null) reemplazó al viejo `findOrCreateFarmer`. NO reintroducir auto-creación de agricultores en el import. `agricultor_alias` tiene índice único por nombre normalizado.
- **⚠️ DEADLOCK LATENTE `ensureLaborTables` (2026-09-03)**: el PRIMER cierre de pilado tras arrancar el backend en frío puede **colgarse**. Causa: `cerrarProcesoProduccion` (dentro de una tx) lee `labor_rates` en `calcularServicioPilado` (ACCESS SHARE) y luego `createProductionWorkerPayments` llama `ensureLaborTables()` que corre `ALTER TABLE labor_rates` (ACCESS EXCLUSIVE) en OTRA conexión del pool → se bloquean mutuamente (una espera en el cliente, PG no lo detecta). Se evita si algo llamó `/labor/rates` o Nómina antes (el ALTER ya es no-op). **Workaround en QA**: pegar `GET /api/v1/labor/rates` antes del primer cierre. Se lanzó una **tarea aparte** para arreglarlo de raíz (correr `ensureLaborTables()` al arrancar el server / migración). Confirmar si se cerró.
- **`sack_movements.ref_batch` vs `ref_cash_movement`**: `ref_batch` es FK a **`processing_batches`** (consumo por pilado) — NO meter ahí un id de otra tabla (viola la FK). El enlace a Caja usa la columna aparte **`ref_cash_movement`** (FK a `cash_movements`). El reverso de compra de sacos y su idempotencia dependen de `ref_cash_movement` + el neto ENTRADA−SALIDA por tipo.
- **Sacos = SOLO Matriz**: `assertMatriz` protege las ESCRITURAS de `/sacks/*` (movements/adjust/precio/purchases) → 403 si el accionista activo no es MATRIZ. En Inventario el bloque de sacos se oculta a socios (`esCeyroActivo`). No reintroducir egresos de sacos per-socio en `inventory_movements`.
- **Gana / rendimiento**: `Gana` reusa `/processing-batches/history` (ya filtra por accionista). Subproductos por división directa; **% Arroz Blanco = EXCEDENTE**: `((QQ Blanco − QQ Cáscara)/QQ Cáscara)×100` truncado 2 dec (135/122.10→10.56%), mostrado como badge por lote (NO como columna). Tabla (`df00a43`): `Concepto | QQ | Precio Unit. | Costo Prod. | Total ($)` (se quitó `% s/ cáscara`). **Tarifa de Pilada = SERVICIO PILADO del SOCIO** (`tarifario_servicio`: socio_id + servicio='PILADO' + precio_por_qq vigente; ROVINSON/STALYN = $3.75). NO es `labor_rates.pilador_per_qq` (eso es pago al trabajador). El backend la expone en `/processing-batches/history` como `pilada_rate_per_qq` por fila (subquery a tarifario_servicio del socio dueño del lote `l.accionista_id`; servicios → NULL). NO hay input manual (se eliminó), solo etiqueta read-only "Tarifa de pilada aplicada: $X.XX (Configuración)". `tarifaPiladaGlobal` se lee SOLO de lotes no-servicio, sin fallback a labor rates (0 si el socio no tiene tarifa; CEYRO matriz no tiene tarifario). Sección cáscara: `Costo Prod. = (Σ QQ Blanco × tarifa socio)/Σ QQ Cáscara`; **`Total ($) fila = (Precio compra + Costo Prod.) × QQ`**; total sección = costoCascara(compra) + costoProdTotal(proceso). Tarjeta "Costo cáscara"/Utilidad siguen usando solo el costo de **compra**. Producción YA NO muestra cuadros (movidos a Gana); Finalizar Lote redirige a Gana. `millingEficiencia`/`productionHistoryOpen` sin uso.

---

## INSTRUCCIÓN PARA LA PRÓXIMA SESIÓN
> Pegar esto al iniciar Claude Code:

```
Continuemos el proyecto BASCULA-ERP. Antes de nada, lee SOLO el archivo
PROJECT_CONTEXT.md (raíz del repo) y NO analices todo el código. Trabaja desde el
checkout MAIN (ignora .claude/worktrees/*).
Contexto rápido: todo en main y pusheado (último 3519c8a). LO MÁS RECIENTE
(2026-09-04, §2s): «GANA» ES AHORA UNA LIQUIDACIÓN FINANCIERA — la cáscara se
desglosa por partida con su PRECIO DE COMPRA (cruzado desde Liquidaciones) y costo;
se CORRIGIÓ el total de cáscara (suma de QQ calificados, no input_paddy_kg/45.359,
el bug 269.18); y cada producto de salida (Blanco, Arrocillo 3/4, Fino, Polvillo)
tiene precio de VENTA editable → Total=QQ×precio, con INGRESO TOTAL PROYECTADO y
Utilidad. Precios en production_yields.precio_venta_{blanco,broken,fine,bran} vía
PATCH /processing-batches/:id/precio-venta {producto,precio} (aislado por accionista).
La sesión 2026-09-03 (§2r) cerró 8 features: (1) EMPAQUE Tulas/Sacos por túnel en Secadoras —
el pago de cuadrilla por "Detectar labores de túnel" usa RECEPCION/BOTADA por QQ
(Tulas) o ENSACADO por saco (Sacos). (2) MIX Tula/Saco POR LÍNEA en Pilado — cada
renglón elige destino (Tula→Selección / Saco); el estibador cobra tula+saco en el
mismo lote (se rompió la XOR). (3) SACOS = inventario EXCLUSIVO de la Matriz:
descuento centralizado + trazabilidad (Lote/Socio), bloque oculto a socios en
Inventario, y guard assertMatriz en las escrituras de /sacks/*. (4) CAJA↔SACOS: el
egreso "Compra de sacos" genera ENTRADA automática al inventario y anularlo la
revierte (col sack_movements.ref_cash_movement). (5) PRECIO base por tipo de saco
(autocompleta en Caja, editable en Inventario). (6) SNAPSHOT de rendimiento por lote
(production_yields.rendimiento_snapshot). (7) Módulo NUEVO «Gana» (Sidebar OPERACIÓN,
entre Producción e Inventario): cuadros de liquidación por lote finalizado, POR
ACCIONISTA, fórmulas de división directa (blanco/cáscara, arrocillos agrupados,
polvillo). Producción YA NO muestra cuadros y al Finalizar Lote redirige a Gana.
Pendientes: (a) DEADLOCK LATENTE de ensureLaborTables (§8; se lanzó tarea aparte —
confirmar si se cerró); (b) ¿reponer la tarjeta verde de eficiencia en vivo en
Producción? No hay ramas abiertas. Migraciones 20260914…20260919 aplicadas en
local; en otro server: git pull → ./deploy.sh (sin auto-deploy).
Pregúntame qué sigue (candidatos en §5).
```
